name: New MCP server
version: 0.0.1
schema: v1
mcpServers:
  - name: Serena MCP
    command: /home/oleg/.local/bin/uvx
    "args": [
      "--from",
      "git+https://github.com/oraios/serena",
      "serena",
      "start-mcp-server",
      "--context",
      "ide",
      "--project",
      "$(pwd)"
    ]