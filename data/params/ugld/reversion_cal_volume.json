{
  "_comment": "VOLUME filter calibration. Forces UseVolume=1; blocks an entry whose bar volume is below avg(prev VolAvgPeriod bars, weekends excluded) × VolMult. Run only if the screen kept UseVolume ON. go run ./cmd/backtest -ticker NVTK -strategy reversion -interval Day1 -calibrate data/params/nvtk/reversion_cal_volume.json -out ./reports/NVTK_volume -months 50 -test-months 12 -metric profit_factor -min-trades 20",
  "phases": [
    {
      "name": "volume",
      "grid": {
        "UseVolume": [1],
        "VolAvgPeriod": [10, 20, 30],
        "VolMult": [1.0, 1.2, 1.5, 2.0, 1.3,1.6,1.7,1.8, 1.9]
      }
    }
  ]
}
