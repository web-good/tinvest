# DIAS под `rsi_pullback` — план подготовки, калибровки и вердикта

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** довести DIAS (Диасофт) до вердикта по стратегии `rsi_pullback`: каталог сеток с осями по
замерам инструмента, пакет параметров, десять тем rolling walk-forward, литерал и заведение в
боевую вселенную пятнадцатым тикером.

**Architecture:** тикер-специфичный каталог `data/params/rsi_pullback/dias/` (десять однотемных
файлов + точка) + сторожевой тест осей в `internal/service/backtest` + пакет параметров
`strategy/dias` (сначала baseline-состояние, затем литерал со снимком) + записи в двух реестрах
(бэктест и живой раннер) + заведение в боевую вселенную + разделы документации. Прогоны идут по
АДАПТИРОВАННОЙ схеме `-months 30 -train-months 10 -test-months 5`: истории у инструмента 30.3
месяца, штатные четыре фолда по 12/6 на ней не помещаются.

**Tech Stack:** Go 1.25, `cmd/backtest` (grid-search + rolling walk-forward), `cmd/pullparity`,
`go test`, `./bin/golangci-lint`, `./bin/mage ci`.

**Spec:** `docs/superpowers/specs/2026-08-22-dias-rsi-pullback-prep-design.md`

## Global Constraints

- Схема прогонов — **`-months 30 -train-months 10 -test-months 5 -metric profit_factor`**,
  `-min-trades 20` (у темы `screen` — `-min-trades 1`). Четыре фолда встык: train 10 месяцев +
  OOS 5 месяцев, шаг 5, всего 10 + 4×5 = 30. Схема АДАПТИРОВАННАЯ, потому что DIAS торгуется с
  2024-02-13 и штатных 36 месяцев у него нет. Следствие, которое надо повторять в каждом
  `_comment` и в доках: **числа DIAS сопоставимы с IVAT (25/9/4), но НЕ построчно с остальным
  каталогом**, где окна 12/6.
- Кэш **перезалит целиком** (`-refresh`, 2026-08-22, до первого замера): в исходном
  `DIAS_Day1.json` было 600 свечей и дыра **2024-06-24 … 2024-12-23** — полугодие отсутствовало,
  143 будних дня были в получасовой серии и не были в дневной. После перезалива:
  `DIAS_Minutes30.json` — **28 127 баров**, окно 2024-02-13 … 2026-08-22; в расчётном 30-месячном
  окне `2024-02-22 … 2026-08-22` — **27 993 бара, из них 20 501 будний**. `DIAS_Day1.json` —
  **745 дневных свечей**, в расчётном окне 738, из них **634 будних**. Дыр длиннее четырёх дней в
  получасовой серии нет; в дневной осталась одна — новогодние каникулы 2025-12-30 … 2026-01-05.
- **Запас истории — 9 дней** (кэш с 2024-02-13, расчётное окно с 2024-02-22). `-months 31`
  упрётся в начало кэша. Повторный `-refresh` во время калибровки НЕ запускать: он сдвинет правую
  границу окна и сделает часть прогонов несравнимой с остальными.
- Планка, объявленная ДО прогонов и не пересматриваемая после: темы `entry` и `trend` **обе** дают
  pooled OOS PF ≥ 1.5 при ≥ 20 сделках; ведущая ось темы (`RSILower` для `entry`, `EMASlow` для
  `trend`) выбрана одинаково в ≥ 3 фолдах из 4. Вырожденный фолд (ни одной убыточной сделки) не
  засчитывается в пользу тикера. Планка считается по дефолтной комиссии бэктеста (круг 0.1%).
  Условие «≥ 5 сделок в каждом фолде» из плана IVAT здесь НЕ применяется: там оно было костылём
  под четырёхмесячные OOS-окна, здесь окна пятимесячные и плотность сделок втрое выше.
- **Заведомо тяжёлого фолда здесь нет — тяжелы все четыре.** У DIAS падают ВСЕ шесть полугодий
  истории (−6.7 / −26.7 / −18.5 / −38.2 / −4.2 / −29.4%), всё окно −79.0%, пик-минимум −85.7%.
  Такого в каталоге не было ни у кого: у ELFV росли два полугодия, у LSNGP росли оба протокольных
  окна. Читается это в обе стороны и обе записываются: завысить лонговый результат режимом нечем,
  но и против роста конфигурация не проверялась вовсе — зеркало риска LSNGP.
- Правило прода, подтверждённое владельцем 2026-08-22 ДО прогонов и применяемое **как есть**:
  литерал ставится и тикер заводится в `RSI_PULLBACK_TICKERS` пятнадцатым даже при непройденной
  планке. **Стоп-условие:** pooled OOS PF принятой точки < 1.0 либо < 20 сделок за расчётное окно
  → остановиться, принести числа владельцу, задачи 12–15 не выполнять.
- **Априор второй по силе в каталоге, и это записано до прогонов.** Скринер
  (`reports/pullback_screen/pullback_screen_Minutes30_20260804_232456.md`, строка 12 из 99
  прошедших): оборот 59 млн ₽, ATR 3.18%, `TradesMed` 25, `PFmed` **1.62**, `Capped` 0/24,
  `SilentCfg` 0/24, плато 71%, **`PFmed HO` 3.35** на 6 сделках. Контрольный прогон
  `core.DefaultParams()` на расчётных 30 месяцах: **153 сделки, PF 1.822, net +75 227 ₽**. Вес
  обоих фактов ограничен ровно так же, как на ELFV, и там это подтвердилось: ELFV взяли с лучшим
  априором каталога, и планку он провалил обеими ключевыми темами. Сильный априор НЕ смягчает
  планку и не сокращает набор тем.
- Замеры инструмента, на которые ссылаются `_comment` сеток (все — будние бары расчётного окна,
  30 месяцев, 20 501 бар):
  - кроссы RSI вниз на уровнях 10/15/20/25/30/35/40: RSI(4) 207/433/773/1173/1636/2148/2577;
    RSI(5) 106/260/490/823/1262/1728/2231; RSI(6) 60/156/333/609/974/1441/1938;
    **RSI(7) 31/100/232/483/782/1243/1702**; RSI(8) 19/70/172/372/655/1060/1550. Для справки
    RSI(3) 460/849/1274/1741/2229/2737/3061 — в сетку не берётся (три бара это шум, правило
    каталога); RSI(9)@10 даёт 8. Планка живого угла каталога — 29 кроссов: **RSI(7)@10 = 31** её
    проходит впритык, **RSI(8)@10 = 19 не проходит**, поэтому ось периода обрывается на 7;
  - кроссы RSI вверх (полоса выхода), RSI(4): 55 — 2658, 60 — 2285, 65 — 1822, 70 — 1389,
    75 — 942, 80 — 618;
  - доля баров с `EMAFast > EMASlow`: **36.7–40.7% на всех 35 парах** (5/50 — 40.6%, 10/100 —
    38.9%, 20/150 — 38.4%, 30/100 — 39.2%, 40/200 — 37.3%). Размах 4.0 п.п. со слабым монотонным
    убыванием по медленному периоду; мёртвых пар нет. Уровень допуска ниже всего каталога (у ELFV
    44–46%) — прямое следствие непрерывного падения цены;
  - дневной ATR(14): медиана **3.05%** цены, p10 2.19, p90 4.48, n=620 — середина каталога,
    практически как ELFV (2.98%). В процентах цены: 0.3 ATR = 0.92%, 0.5 = 1.53%, 0.7 = 2.14%,
    1.0 = 3.05%, 1.3 = 3.97%. Круг издержек 0.1% съедает на стопе 0.3 ATR — 10.9% риска, 0.5 —
    6.6%, 0.7 — 4.7%, 1.0 — 3.3%, 1.3 — 2.5%;
  - **шаг цены 0.5 ₽** (все котировки окна кратны ему; медианная цена 3033.5) = **0.0165% цены**,
    в 2.4 раза дешевле ELFV. Бэктест наливает по закрытию бара и спред не моделирует, поэтому
    реальный круг издержек ≈ **0.13%**, а не 0.2%, как на ELFV. При стопе 0.3 ATR это **14.1%**
    риска — ПОД чертой 17%, по которой строку 0.3 вырезали из `domrf/` и `elfv/`, поэтому здесь
    она в сетке остаётся. Замер чувствительности (дефолты, 153 сделки): круг 0.1% → PF 1.822 /
    net 75 227; 0.2% → PF 1.567 / net 50 509; 0.3% → PF 1.336 / net 29 421;
  - выживаемость стопа (доля будних дней, чей размах достаёт уровня, n=620): 0.3 — 99.2%,
    0.5 — 86.1%, 0.7 — 65.6%, 0.8 — 55.6%, 1.0 — 36.1%, 1.25 — 19.5%, **1.3 — 17.3%**,
    1.5 — 12.4%;
  - день ко второму бару: медиана **0.30 ATR**; медиана размаха дня к текущему бару 0.68 ATR
    (n = 19 815). Ветка «свежий день» ловит 5.2% баров при 0.2, 7.7% при 0.25, **10.8% при 0.3**,
    15.1% при 0.35, 20.3% при 0.4, 31.3% при 0.5; ветка «день исчерпан» — 58.3% при 0.6,
    **48.4% при 0.7**, 40.4% при 0.8, 33.3% при 0.9, 26.5% при 1.0, **21.1% при 1.1**, 15.3% при
    1.25, 14.1% при 1.3, **9.4% при 1.5**. Сам гейт стоит дорого и работает: без него
    (`UseDayATRGate = 0`, прочее дефолт) 30 месяцев дают **338 сделок при PF 1.348** против 153
    при 1.822;
  - объёмный гейт в СДЕЛКАХ (точечные прогоны `-params` на дефолтах, `VolBaseDays = 14`,
    `VolLookbackBars = 3`, полные 30 месяцев): выключен → 153 сделки, PF 1.822; `VolMult` 1.2 →
    114, PF 1.765; **1.5 → 102, PF 1.894** (максимум оси); 2.0 → 88, PF 1.689; 2.5 → 78,
    PF 1.410; 3.0 → 65, PF 1.313; 3.5 → 57, PF 1.258; 4.0 → 49, PF 1.065. На 10-месячное
    обучающее окно это 38 / 34 / 29 / 26 / 22 / 19 / 16 сделок. Максимум ЕДВА выше baseline без
    гейта (1.894 против 1.822 ценой трети выборки), дальше спад монотонный — случай SIBN, а не
    ELFV;
  - база объёмного гейта при `VolMult = 1.5`: 3 дня → 113 сделок, PF 1.674; 5 → 114, PF 1.747;
    **10 → 108, PF 1.921**; 14 → 102, PF 1.894. Длинные базы лучше коротких;
  - **окно объёмного гейта** (`VolMult = 2.0`, `VolBaseDays = 14`): `VolLookbackBars` 1 → 69
    сделок, PF 1.294; 2 → 80, PF 1.510; **3 (дефолт ядра) → 88, PF 1.689**; 5 → 98, PF 1.867;
    8 → 111, PF 1.819; **12 → 121, PF 1.915** (максимум); 16 → 126, PF 1.810. Рост монотонный до
    12 — дефолт ядра хуже максимума на 0.23 PF и на 33 сделки. На ELFV та же ось имела максимум
    ровно на дефолте, и вывод был «на других тикерах тему не заводить» — DIAS его отменяет;
  - **покрытие баров**: медиана 34 бара в буднем дне, p10 = **19**, минимум 17; у **77 будних дней
    из 648 (11.9%) баров меньше двадцати**; 838 будних баров (4.1%) имеют нулевой размах
    (`High == Low`); баров с нулевым объёмом нет. Вдвое реже, чем у ELFV (18.9%), но втрое чаще,
    чем на ликвидных тикерах каталога;
  - оборот (лот 1, сверен со средним скринера 59 млн — расхождение объясняется разными окнами,
    30 месяцев против 36): медиана **42.6 млн ₽/день**, p10 13.6, p90 134.8, среднее 67.0 —
    уровень IVAT (43) и LENT (38), лучше ELFV (25.1). Гейт отбора скринера в 50 млн DIAS проходит
    ТОЛЬКО средним;
  - гэпы открытия к закрытию предыдущего буднего дня (n=633): медиана +0.03%, p5 −0.73%,
    p95 +1.20%; хуже −3% **два** гэпа: 2024-06-13 −5.67% (дивидендная отсечка) и 2026-03-23
    −5.54%; лучше +3% — четыре. Вдвое реже, чем у SIBN (пять), но риск отсечки есть, в отличие
    от ELFV, где его нет вовсе;
  - цена в окне: 5597 → 1197 ₽, медиана **3033.5**, минимум 905, максимум 6519.5;
  - режим: всё окно **−79.0%**, пик-минимум **−85.7%**; полугодия −6.7% / −26.7% / −18.5% /
    −38.2% / −4.2% / −29.4% — **растущих НЕТ ни одного**.
- Отчёты прогонов пишутся в `./reports/DIAS_<тема>` (каталог `reports/` в `.gitignore`).
- Коммиты делать по завершении каждой задачи; сообщения на русском, в стиле существующей истории
  (`feat(rsi_pullback): ...`). Ветка — текущая `feat/dias-pullback-prep` (отведена от
  `feat/elfv-pullback-prep`, HEAD `c9a88b8`; дизайн лежит коммитом `ab89138`).

---

### Task 1: Каталог сеток DIAS со сторожевым тестом осей

**Files:**
- Create: `data/params/rsi_pullback/dias/cal_screen.json`, `cal_entry.json`, `cal_trend.json`,
  `cal_day.json`, `cal_day_spent.json`, `cal_volume.json`, `cal_vol_window.json`, `cal_risk.json`,
  `cal_exit.json`, `cal_trail.json`
- Test: `internal/service/backtest/rsi_pullback_dias_grid_test.go`

**Interfaces:**
- Consumes: хелперы `rsiPullbackTickerGrid(t, ticker, file)`
  (`internal/service/backtest/rsi_pullback_grid_test.go:40`), `sameSet(got, want...)`
  (`rsi_pullback_reni_grid_test.go:17`) и `containsValue(axis, want)`
  (`rsi_pullback_ivat_grid_test.go:88`). **Все три уже объявлены в пакете — НЕ переобъявлять**,
  иначе пакет не соберётся.
- Produces: каталог `dias/`, на который ссылаются все прогоны Task 3–10, и функцию
  `diasGrid(t, file)`.

- [ ] **Step 1: Написать падающий тест осей**

Создать `internal/service/backtest/rsi_pullback_dias_grid_test.go`:

```go
package backtest

import "testing"

// diasGrid читает файл сеток DIAS через общий хелпер.
func diasGrid(t *testing.T, file string) map[string][]float64 {
	t.Helper()
	return rsiPullbackTickerGrid(t, "dias", file)
}

// TestDIASGridsPinTheirMeasuredAxes сторожит оси каталога dias/. Каталог собран 2026-08-22
// копированием формы elfv/ с пересадкой каждой оси на замеры самого DIAS (28 127 30-минутных
// баров в кэше, 27 993 в расчётном окне 30 месяцев с 2024-02-22, из них 20 501 будний; кэш
// ПЕРЕЗАЛИТ целиком, потому что в дневной серии отсутствовало полугодие 2024-06-24…2024-12-23).
// Четыре решения отличают этот каталог от образца, и каждое опирается на замер:
//
//   - RSIPeriod ОБОРВАН на 7. Планка живого угла в каталоге — 29 кроссов (столько дал слабейший
//     угол LSNGP, объявленный мёртвым; у RENI было 23). RSI(7)@10 даёт 31 будний кросс за 30
//     месяцев и проходит впритык, RSI(8)@10 даёт 19 и не проходит. То же решение, что на ELFV,
//     обратное SIBN (там 8@10 давал 34).
//   - VolMult СУЖЕН до 2.5 — обратное тому, что сделали на ELFV. Точечные прогоны: гейт даёт
//     максимум 1.894 на 102 сделках при множителе 1.5, что ЕДВА выше baseline без гейта (1.822
//     на 153 сделках) и стоит трети выборки, а дальше PF падает монотонно (2.0 -> 1.689,
//     2.5 -> 1.410, 3.0 -> 1.313, 4.0 -> 1.065). Значение 3.0 оставляет 65 сделок = 22 на
//     10-месячное обучающее окно при -min-trades 20 и PF ниже baseline: такая строка может
//     победить только процедурным артефактом. На ELFV та же ось расширялась до 3.0, потому что
//     там все 24 комбинации решётки били baseline.
//   - VolLookbackBars РАСШИРЕН до 16 — впервые в каталоге ось уходит далеко за дефолт ядра 3.
//     Точечные прогоны (VolMult 2.0, база 14): 1 -> 69 сделок PF 1.294, 2 -> 80/1.510,
//     3 -> 88/1.689, 5 -> 98/1.867, 8 -> 111/1.819, 12 -> 121/1.915, 16 -> 126/1.810. Рост
//     монотонный до 12, дефолт ядра хуже максимума на 0.23 PF и на 33 сделки. Край 16 нужен,
//     чтобы разворот кривой попал ВНУТРЬ сетки, а не на её границу. Инструментальное основание:
//     11.9% будних дней несут меньше двадцати баров, поэтому окно в 12 баров здесь — треть
//     торгового дня, а не «полтора часа», как на ликвидной бумаге.
//   - Строка StopDailyATR = 0.3 ВОЗВРАЩЕНА, хотя у domrf/ и elfv/ она вырезана. Шаг цены на DIAS
//     равен 0.5 ₽ = 0.0165% медианной цены 3033.5, то есть втрое дешевле, чем на ELFV (0.039%).
//     Реальный круг издержек ≈ 0.13%, и при стопе 0.3 ATR (0.92% цены) он съедает 14.1% риска —
//     под чертой 17%, по которой строку резали. Замер чувствительности: круг 0.1% -> PF 1.822,
//     0.2% -> 1.567, 0.3% -> 1.336.
func TestDIASGridsPinTheirMeasuredAxes(t *testing.T) {
	screen := diasGrid(t, "cal_screen.json")
	for _, field := range []string{"UseDayATRGate", "UseVolume"} {
		if got := screen[field]; !sameSet(got, 0, 1) {
			t.Errorf("cal_screen.json: %s = %v, want ровно {0,1} — тема меряет цену каждого гейта в сделках", field, got)
		}
	}

	entry := diasGrid(t, "cal_entry.json")
	// Выше 50 отката нет по определению: 50 — средняя линия осциллятора.
	for _, v := range entry["RSILower"] {
		if v > 50 {
			t.Errorf("cal_entry.json свипует RSILower=%v: выше 50 отката нет по определению", v)
		}
	}
	// Уровень 10 обязан остаться: на DIAS он живой — RSI(4)@10 даёт 207 будних кроссов за 30
	// месяцев, слабейший угол сетки RSI(7)@10 — 31, выше планки живого угла 29.
	if !containsValue(entry["RSILower"], 10) {
		t.Errorf("cal_entry.json: RSILower = %v, не содержит 10 — на DIAS это живой угол (207 кроссов RSI(4)@10)", entry["RSILower"])
	}
	// RSIPeriod короче 4 покупает шум, а не откат: RSI(3)@10 даёт 460 кроссов против 207 у RSI(4).
	for _, v := range entry["RSIPeriod"] {
		if v < 4 {
			t.Errorf("cal_entry.json свипует RSIPeriod=%v: короче 4 это шум, а не откат", v)
		}
		// Верхний край 7: RSI(8)@10 даёт 19 кроссов за 30 месяцев — ниже планки живого угла 29,
		// по которой угол объявляли мёртвым на LSNGP и RENI.
		if v > 7 {
			t.Errorf("cal_entry.json свипует RSIPeriod=%v: слабейший угол RSI(8)@10 даёт 19 кроссов, ось мертва за 7", v)
		}
	}
	if !containsValue(entry["RSIPeriod"], 7) {
		t.Errorf("cal_entry.json: RSIPeriod = %v, не содержит 7 — это последний живой угол (31 кросс RSI(7)@10)", entry["RSIPeriod"])
	}

	trend := diasGrid(t, "cal_trend.json")
	// Ось тренда режется только вместе с этим тестом. На DIAS доля баров с EMAFast > EMASlow
	// укладывается в 36.7-40.7% на ВСЕХ 35 парах: выбор пары меняет не объём допуска, а то,
	// какие именно бары в него попадают. Уровень ниже всего каталога (у ELFV 44-46%) — прямое
	// следствие того, что бумага падала все 30 месяцев.
	if !containsValue(trend["EMASlow"], 200) {
		t.Errorf("cal_trend.json: EMASlow = %v, не содержит 200 — медленный край оси обязан остаться, допуск у него 36.7-37.3%%", trend["EMASlow"])
	}
	for _, fast := range trend["EMAFast"] {
		for _, slow := range trend["EMASlow"] {
			if fast >= slow {
				t.Errorf("cal_trend.json: пара EMAFast=%v EMASlow=%v вырождена — быстрая обязана быть быстрее медленной", fast, slow)
			}
		}
	}

	risk := diasGrid(t, "cal_risk.json")
	// Строка 0.3 ВОЗВРАЩЕНА в сетку, в отличие от domrf/ и elfv/: шаг цены 0.5 ₽ = 0.0165% цены,
	// реальный круг издержек ≈ 0.13% и съедает на этом стопе 14.1% риска — под чертой 17%.
	if !containsValue(risk["StopDailyATR"], 0.3) {
		t.Errorf("cal_risk.json: StopDailyATR = %v, не содержит 0.3 — на DIAS шаг цены втрое дешевле, чем на ELFV, и строка живая (14.1%% риска)", risk["StopDailyATR"])
	}
	for _, v := range risk["StopDailyATR"] {
		if v < 0.3 {
			t.Errorf("cal_risk.json свипует StopDailyATR=%v: уже 0.3 ATR круг издержек съедает больше 17%% риска даже при дешёвом шаге цены", v)
		}
		// Верхний край 1.3: уровня 1.5 ATR достаёт лишь 12.4% дней, такой стоп перестаёт быть
		// защитой и становится способом вытеснить убыток в RSI-выход.
		if v > 1.3 {
			t.Errorf("cal_risk.json свипует StopDailyATR=%v: до него доходит 12%% дней — это не стоп", v)
		}
	}

	day := diasGrid(t, "cal_day.json")
	// Ветка «свежий день» ловит 10.8% будних баров при пороге 0.3 и 31.3% при 0.5; ноль в оси —
	// это выключенная ветка, и она обязана остаться, потому что на всех прод-тикерах каталога
	// победил именно ноль.
	if !containsValue(day["FreshDayATR"], 0) {
		t.Errorf("cal_day.json: FreshDayATR = %v, не содержит 0 — выключенная ветка обязана быть в сетке", day["FreshDayATR"])
	}
	// Верхний край ветки «день исчерпан» ОСТАВЛЕН на 1.5: через него проходит 9.4% баров —
	// больше, чем на SVAV (7.7%), где край держался, и заметно больше, чем на SIBN (5.3%).
	if !containsValue(day["SpentDayATR"], 1.5) {
		t.Errorf("cal_day.json: SpentDayATR = %v, не содержит 1.5 — на DIAS край живой (9.4%% баров)", day["SpentDayATR"])
	}

	daySpent := diasGrid(t, "cal_day_spent.json")
	if !containsValue(daySpent["SpentDayATR"], 1.5) {
		t.Errorf("cal_day_spent.json: SpentDayATR = %v, не содержит 1.5 — край живой по замеру 9.4%% баров", daySpent["SpentDayATR"])
	}
	// Уплотнение оси живыми уровнями — часть решения, а не случайность.
	for _, want := range []float64{0.7, 1.1} {
		if !containsValue(daySpent["SpentDayATR"], want) {
			t.Errorf("cal_day_spent.json: SpentDayATR = %v, не содержит %v — уровень живой (48.4%% и 21.1%% баров)", daySpent["SpentDayATR"], want)
		}
	}

	volume := diasGrid(t, "cal_volume.json")
	// Край 2.5 — СУЖЕНИЕ против elfv/, где ось доходила до 3.0. Держится на форме кривой и на
	// счёте сделок: 3.0 оставляет 65 сделок (22 на 10-месячное обучающее окно) при PF 1.313,
	// ниже baseline без гейта (1.822).
	for _, v := range volume["VolMult"] {
		if v > 2.5 {
			t.Errorf("cal_volume.json свипует VolMult=%v: при 3.0 остаётся 65 сделок (22 на train) и PF 1.313, ниже baseline 1.822", v)
		}
	}
	// Максимум оси стоит на 1.5 и обязан быть в сетке.
	if !containsValue(volume["VolMult"], 1.5) {
		t.Errorf("cal_volume.json: VolMult = %v, не содержит 1.5 — это максимум оси (102 сделки, PF 1.894)", volume["VolMult"])
	}

	// Вторая в каталоге тема этой оси (первая — ELFV) и первая, где ось уходит далеко за дефолт
	// ядра: PF растёт монотонно от 1.294 при окне 1 до 1.915 при окне 12, дефолт 3 даёт 1.689.
	volWindow := diasGrid(t, "cal_vol_window.json")
	if !sameSet(volWindow["UseVolume"], 1) {
		t.Errorf("cal_vol_window.json: UseVolume = %v, want ровно {1} — тема меряет ОКНО гейта, а выключенный гейт окна не имеет", volWindow["UseVolume"])
	}
	for _, v := range volWindow["VolLookbackBars"] {
		if v < 1 {
			t.Errorf("cal_vol_window.json свипует VolLookbackBars=%v: окно меньше одного бара выключает гейт молча, а не меряет его", v)
		}
		// Верхний край 16: при 126 сделках из 153 возможных гейт с таким окном почти неотличим
		// от выключенного, дальше тема меряла бы не окно, а отсутствие гейта.
		if v > 16 {
			t.Errorf("cal_vol_window.json свипует VolLookbackBars=%v: гейт с таким окном неотличим от выключенного (126 сделок из 153)", v)
		}
	}
	// Дефолт ядра — опорная точка сравнения, максимум замера — то, ради чего ось расширена.
	for _, want := range []float64{3, 12} {
		if !containsValue(volWindow["VolLookbackBars"], want) {
			t.Errorf("cal_vol_window.json: VolLookbackBars = %v, не содержит %v (дефолт ядра 3 — опорная точка, 12 — максимум замера PF 1.915)", volWindow["VolLookbackBars"], want)
		}
	}

	exit := diasGrid(t, "cal_exit.json")
	// Полоса выхода рабочая на всём диапазоне: кроссов вверх RSI(4) от 2658 (55) до 618 (80).
	for _, v := range exit["RSIUpper"] {
		if v <= 50 {
			t.Errorf("cal_exit.json свипует RSIUpper=%v: выход обязан стоять выше средней линии", v)
		}
	}
}
```

- [ ] **Step 2: Запустить тест и убедиться, что он падает**

Run: `go test ./internal/service/backtest/ -run TestDIASGridsPinTheirMeasuredAxes -v`
Expected: FAIL — каталога `data/params/rsi_pullback/dias/` ещё нет, хелпер падает на чтении файла.

- [ ] **Step 3: Создать десять файлов сеток**

Каждый файл — объект с `_comment` и массивом `phases`. Сетки даны ниже дословно. `_comment`
пишется по образцу `elfv/` и обязан содержать четыре части: (1) что тема меряет и сколько в ней
прогонов; (2) замер из Global Constraints, из которого получена каждая ось этого файла, с прямым
указанием, почему край оси стоит там, где стоит; (3) команду запуска целиком (схема
`-months 30 -train-months 10 -test-months 5` с причиной адаптации); (4) пустое место под строку
«РЕЗУЛЬТАТ ПРОГОНА 2026-08-22: …», которую заполняют задачи 3–10.

**Жёсткое требование пакета, а не стиля:** `TestRSIPullbackCalFilesValid`
(`internal/service/backtest/rsi_pullback_grid_test.go:89`) падает, если `_comment` файла не
содержит его собственный путь вида `dias/cal_entry.json`. Полная команда запуска с
`data/params/rsi_pullback/dias/cal_entry.json` это условие выполняет; проверка ловит `_comment`,
скопированный у соседнего тикера без правки пути.

`cal_screen.json` — 4 прогона:
```json
{"phases": [{"name": "screen", "grid": {"UseDayATRGate": [0, 1], "UseVolume": [0, 1]}}]}
```

`cal_entry.json` — 168 прогонов (ось `RSIPeriod` оборвана на 7):
```json
{"phases": [{"name": "entry", "grid": {
  "RSIUpper": [55, 60, 65, 70, 75, 80],
  "RSIPeriod": [4, 5, 6, 7],
  "RSILower": [10, 15, 20, 25, 30, 35, 40]
}}]}
```

`cal_trend.json` — 35 прогонов (канон):
```json
{"phases": [{"name": "trend", "grid": {
  "EMAFast": [5, 10, 20, 30, 40],
  "EMASlow": [50, 70, 100, 120, 150, 170, 200]
}}]}
```

`cal_day.json` — 24 прогона:
```json
{"phases": [{"name": "day", "grid": {
  "UseDayATRGate": [1],
  "FreshDayATR": [0, 0.3, 0.4, 0.5],
  "SpentDayATR": [0.6, 0.8, 0.9, 1.0, 1.25, 1.5]
}}]}
```

`cal_day_spent.json` — 8 прогонов:
```json
{"phases": [{"name": "day_spent", "grid": {
  "UseDayATRGate": [1],
  "FreshDayATR": [0],
  "SpentDayATR": [0.6, 0.7, 0.8, 0.9, 1.0, 1.1, 1.25, 1.5]
}}]}
```

`cal_volume.json` — 20 прогонов (ось `VolMult` сужена до 2.5):
```json
{"phases": [{"name": "volume", "grid": {
  "UseVolume": [1],
  "VolMult": [1.0, 1.2, 1.5, 2.0, 2.5],
  "VolBaseDays": [3, 5, 10, 14]
}}]}
```

`cal_vol_window.json` — 21 прогон (ось `VolLookbackBars` расширена до 16):
```json
{"phases": [{"name": "vol_window", "grid": {
  "UseVolume": [1],
  "VolLookbackBars": [1, 2, 3, 5, 8, 12, 16],
  "VolMult": [1.2, 1.5, 2.0]
}}]}
```

`cal_risk.json` — 35 прогонов (строка стопа 0.3 возвращена):
```json
{"phases": [{"name": "risk", "grid": {
  "StopDailyATR": [0.3, 0.5, 0.7, 1.0, 1.3],
  "TPDailyATR": [0.5, 0.6, 0.8, 1.0, 1.5, 2.0, 2.5]
}}]}
```

`cal_exit.json` — 6 прогонов (канон):
```json
{"phases": [{"name": "exit", "grid": {"RSIUpper": [55, 60, 65, 70, 75, 80]}}]}
```

`cal_trail.json` — 12 прогонов (канон):
```json
{"phases": [{"name": "trail", "grid": {
  "UseRSIExit": [0, 1],
  "UseTrail": [1],
  "TrailDailyATR": [0, 0.3, 0.5, 0.7, 1.0, 1.3]
}}]}
```

- [ ] **Step 4: Запустить тесты пакета**

Run: `go test ./internal/service/backtest/ -run 'TestDIASGrids|TestRSIPullbackCalFilesValid|TestRSIPullbackGridControlPoints' -v`
Expected: PASS. `TestRSIPullbackGridControlPoints` проверяет, что самая широкая цель шире самого
широкого стопа: 2.5 > 1.3 — выполняется.

- [ ] **Step 5: Коммит**

```bash
git add data/params/rsi_pullback/dias internal/service/backtest/rsi_pullback_dias_grid_test.go
git commit -m "feat(rsi_pullback): сетки DIAS с замеренными осями и расширенным окном объёма"
```

---

### Task 2: Пакет `strategy/dias` в состоянии «калибровка не проводилась»

**Files:**
- Create: `internal/service/trading_strategy/rsi_pullback/strategy/dias/dias.go`
- Create: `internal/service/trading_strategy/rsi_pullback/strategy/dias/dias_test.go`
- Modify: `internal/service/backtest/rsi_pullback_registry.go` (импорт + запись в карту)
- Modify: `internal/service/backtest/rsi_pullback_registry_test.go` (сторожевой тест baseline)

**Interfaces:**
- Consumes: `core.Params`, `core.DefaultParams()` из
  `internal/service/trading_strategy/rsi_pullback/strategy/core`.
- Produces: `dias.Ticker` (константа `"DIAS"`) и `dias.DefaultParams() core.Params` — их используют
  Task 12 (литерал), Task 13 (реестр живого раннера) и Task 14 (вселенная).

- [ ] **Step 1: Написать падающий тест baseline-состояния**

Создать `internal/service/trading_strategy/rsi_pullback/strategy/dias/dias_test.go`:

```go
package dias

import (
	"testing"

	"tinvest/internal/service/trading_strategy/rsi_pullback/strategy/core"
)

// Пакет заведён 2026-08-22 ДО калибровки: он обязан отслеживать baseline, чтобы правка дефолтов
// доходила до тикера, а не расходилась с ним молча. Тест держит это состояние и подлежит замене
// снимком литерала ровно тогда, когда калибровка закончится (задача 12 плана).
func TestParamsTrackTheBaseline(t *testing.T) {
	if got, want := DefaultParams(), core.DefaultParams(); got != want {
		t.Fatalf("DIAS ещё не откалиброван, параметры обязаны совпадать с baseline:\n got: %+v\nwant: %+v", got, want)
	}
}

func TestTickerIsDIAS(t *testing.T) {
	if Ticker != "DIAS" {
		t.Fatalf("Ticker = %q, want DIAS", Ticker)
	}
}
```

- [ ] **Step 2: Запустить тест и убедиться, что он падает**

Run: `go test ./internal/service/trading_strategy/rsi_pullback/strategy/dias/ -v`
Expected: FAIL — пакета нет, сборка не проходит.

- [ ] **Step 3: Создать пакет**

`internal/service/trading_strategy/rsi_pullback/strategy/dias/dias.go`:

```go
// Package dias supplies the ticker and rsi_pullback Params for DIAS (ПАО «Диасофт»).
//
// СОСТОЯНИЕ: калибровка не проводилась. Пакет возвращает core.DefaultParams(), то есть
// отслеживает baseline: правка дефолтов ядра доходит до этого тикера. Ставить DIAS в боевую
// вселенную RSI_PULLBACK_TICKERS в таком состоянии нельзя — торговля пошла бы параметрами,
// которые на этом инструменте никогда не проверялись.
//
// Что известно об инструменте до прогонов (кэш перезалит 2026-08-22, 28 127 30-минутных баров,
// расчётное окно 30 месяцев с 2024-02-22 — 27 993 бара, 20 501 будний):
//
//   - КЭШ БЫЛ БИТЫМ, И ЭТО НАШЛИ СЛУЧАЙНО. В исходном DIAS_Day1.json было 600 свечей и дыра
//     2024-06-24…2024-12-23: полугодие дневных баров отсутствовало целиком, 143 будних дня были
//     в получасовой серии и не были в дневной. Дневная серия несёт ATR, который сайзит стоп и
//     цель, и размах дня, на котором стоит двусторонний дневной гейт, — калибровка на таком кэше
//     считала бы половину 2024 года без дневного ATR. После -refresh: 745 дневных свечей, 634
//     будних в расчётном окне, дыр нет. Процедурное следствие для каталога: перед калибровкой
//     нового тикера проверять НЕПРЕРЫВНОСТЬ обеих серий, а не только глубину истории.
//   - ИСТОРИИ 30.3 МЕСЯЦА (IPO 2024-02-13), поэтому штатный протокол §8 docs/rsi_pullback/
//     strategy.md (-months 36 -train-months 12 -test-months 6) неисполним. Прогоны идут по
//     АДАПТИРОВАННОЙ схеме -months 30 -train-months 10 -test-months 5: четыре фолда встык,
//     train 10 + 4×OOS 5 = 30. Прецедент — IVAT (25/9/4). Числа DIAS сопоставимы с IVAT, но НЕ
//     построчно с остальным каталогом.
//   - РЕЖИМ: ПАДАЮТ ВСЕ ШЕСТЬ ПОЛУГОДИЙ — такого в каталоге не было ни у кого. Всё окно −79.0%,
//     пик-минимум −85.7%, полугодия −6.7% / −26.7% / −18.5% / −38.2% / −4.2% / −29.4%. У ELFV,
//     самого жёсткого до сих пор, росли два полугодия. Читается в обе стороны: завысить лонговый
//     результат режимом здесь НЕЧЕМ (baseline PF 1.822 получен на бумаге, потерявшей 79%), но и
//     против роста конфигурация не проверялась ВОВСЕ — зеркало риска LSNGP, у которого не было
//     падающего окна.
//   - АПРИОР ВТОРОЙ ПО СИЛЕ В КАТАЛОГЕ, и это записано ДО прогонов. Скринер: PFmed 1.62, плато
//     71%, PFmed HO 3.35 на 6 сделках. Контрольный прогон baseline на 30 месяцах: 153 сделки,
//     PF 1.822, net +75 227. Вес обоих фактов ограничен: колонка PFmed HO предсказывает плохо в
//     обе стороны (WUSH с 4.24 планку провалил, LSNGP с 0.99 вытянул девять тем из девяти), а
//     baseline считается на полной истории без walk-forward — он показывает лишь, что дефолты
//     ядра стоят не в мёртвой зоне (прецедент NVTK). На ELFV сильный априор уже не спас планку.
//   - ШАГ ЦЕНЫ ДЕШЁВЫЙ: 0.5 ₽ при медианной цене 3033.5 = 0.0165% цены, в 2.4 раза дешевле ELFV.
//     Бэктест наливает по закрытию бара и спред не моделирует, но реальный круг издержек здесь
//     ≈ 0.13%, а не 0.2%, как на ELFV. Замер чувствительности: круг 0.1% -> PF 1.822,
//     0.2% -> 1.567, 0.3% -> 1.336. Прямое следствие для сеток: строка StopDailyATR = 0.3,
//     вырезанная у domrf/ и elfv/, здесь ВОЗВРАЩЕНА — она отдаёт издержкам 14.1% риска, под
//     чертой 17%.
//   - ЛИКВИДНОСТЬ ТОНКАЯ: оборот медианой 42.6 млн ₽/день при p10 = 13.6 млн (уровень IVAT 43 и
//     LENT 38; у ELFV 25.1, у SIBN 519). Гейт отбора скринера в 50 млн DIAS проходит ТОЛЬКО
//     средним (67.0 млн). Размер живой позиции обязан соотноситься с медианой, а не со средним.
//   - ДЫРЫ В БАРАХ: медиана 34 бара в буднем дне, p10 = 19, минимум 17; у 77 дней из 648 (11.9%)
//     баров меньше двадцати; 4.1% баров имеют нулевой размах. Прямое следствие: ручка
//     VolLookbackBars, задающая окно «последние N будних баров», меряет здесь не только время, но
//     и ликвидность — и её замер это подтверждает: PF растёт монотонно от 1.294 при окне 1 до
//     1.915 при окне 12, тогда как дефолт ядра 3 даёт 1.689. Под неё заведена тема
//     cal_vol_window.json с осью, расширенной до 16.
//   - ОБЪЁМНЫЙ ГЕЙТ ОТБИРАЕТ СЛАБО: максимум оси VolMult стоит на 1.5 (102 сделки, PF 1.894) и
//     едва превышает baseline без гейта (153 сделки, PF 1.822), дальше спад монотонный
//     (2.0 -> 1.689, 2.5 -> 1.410, 3.0 -> 1.313). Случай SIBN, а не ELFV, поэтому ось сужена до
//     2.5, а не расширена до 3.0.
//   - ДНЕВНОЙ ГЕЙТ СТОИТ ДОРОГО И РАБОТАЕТ: без него 30 месяцев дают 338 сделок при PF 1.348
//     против 153 при 1.822.
//   - ТРЕНДОВЫЙ ДОПУСК: доля баров с EMAFast > EMASlow укладывается в 36.7-40.7% на всех 35 парах
//     сетки, размах 4.0 п.п., мёртвых пар нет. Уровень ниже всего каталога (у ELFV 44-46%) —
//     прямое следствие непрерывного падения цены.
//   - ДИВИДЕНДНАЯ ОТСЕЧКА ЕСТЬ, НО РЕДКАЯ: за 30 месяцев два гэпа хуже −3% (2024-06-13 −5.67%,
//     отсечка, и 2026-03-23 −5.54%). Вдвое реже, чем у SIBN (пять), но риск ночного удержания
//     здесь присутствует, в отличие от ELFV, где его нет вовсе.
//
// Сетки калибровки лежат в data/params/rsi_pullback/dias/, их оси прибиты
// internal/service/backtest/rsi_pullback_dias_grid_test.go.
package dias

import "tinvest/internal/service/trading_strategy/rsi_pullback/strategy/core"

// Ticker is the instrument this package configures.
const Ticker = "DIAS"

// DefaultParams returns the strategy baseline: DIAS is not calibrated yet.
func DefaultParams() core.Params {
	return core.DefaultParams()
}
```

- [ ] **Step 4: Зарегистрировать тикер в реестре бэктеста**

В `internal/service/backtest/rsi_pullback_registry.go` добавить импорт
`rsipullbackdias "tinvest/internal/service/trading_strategy/rsi_pullback/strategy/dias"` рядом с
остальными (образец — строка 9, импорт ELFV) и строку карты рядом с записью ELFV (строка 50):

```go
	rsipullbackdias.Ticker:  rsiPullbackBindingFor(rsipullbackdias.Ticker, rsipullbackdias.DefaultParams),
```

- [ ] **Step 5: Добавить сторожевой тест baseline в реестр бэктеста**

В `internal/service/backtest/rsi_pullback_registry_test.go`:

```go
// TestRSIPullbackDIASTracksBaseline держит состояние «калибровка не проводилась»: пакет
// strategy/dias заведён 2026-08-22 под будущий литерал, и до конца калибровки обязан возвращать
// core.DefaultParams(). Тест заменяется снимком литерала в тот день, когда литерал появится, —
// ровно так это было с reni, fesh, wush, lsngp, nvtk, ivat, svav, sibn и elfv.
func TestRSIPullbackDIASTracksBaseline(t *testing.T) {
	b, ok := rsiPullbackRegistry["DIAS"]
	if !ok {
		t.Fatal("DIAS отсутствует в rsiPullbackRegistry: тикер провалится в generic-ветку")
	}
	p, pok := b.DefaultParams().(core.Params)
	if !pok {
		t.Fatalf("DIAS: DefaultParams() вернул %T, want core.Params", b.DefaultParams())
	}
	if p != core.DefaultParams() {
		t.Fatalf("DIAS отклонился от baseline до калибровки: %+v", p)
	}
	if got := b.Build(p).Ticker(); got != "DIAS" {
		t.Fatalf("Ticker() = %q, want DIAS", got)
	}
}
```

- [ ] **Step 6: Запустить тесты**

Run: `go test ./internal/service/trading_strategy/rsi_pullback/... ./internal/service/backtest/ -run 'DIAS|RSIPullback' -v`
Expected: PASS. Тест
`internal/service/trading_strategy/rsi_pullback/live/registry_test.go:TestBaselineTrackingTickersStayOutOfTheDefaultUniverse`
обязан остаться зелёным: DIAS пока не в реестре живого раннера и не в дефолтной вселенной.

- [ ] **Step 7: Коммит**

```bash
git add internal/service/trading_strategy/rsi_pullback/strategy/dias internal/service/backtest/rsi_pullback_registry.go internal/service/backtest/rsi_pullback_registry_test.go
git commit -m "feat(rsi_pullback): пакет и реестр DIAS в состоянии «калибровка не проводилась»"
```

---

### Task 3: Тема `screen` — цена двух опциональных гейтов

**Files:**
- Modify: `data/params/rsi_pullback/dias/cal_screen.json` (дописать результат в `_comment`)

**Interfaces:**
- Consumes: каталог сеток из Task 1, пакет из Task 2.
- Produces: знание, сколько сделок стоит каждый гейт — им пользуются задачи 6, 7 и 8 при разборе.

- [ ] **Step 1: Прогнать тему**

```bash
go run ./cmd/backtest -ticker DIAS -strategy rsi_pullback -interval Minutes30 \
  -calibrate data/params/rsi_pullback/dias/cal_screen.json -out ./reports/DIAS_screen \
  -months 30 -train-months 10 -test-months 5 -min-trades 1 -metric profit_factor
```

- [ ] **Step 2: Прочитать отчёт**

Открыть свежий файл в `./reports/DIAS_screen/`. Выписать: pooled OOS PF и счёт сделок каждой из
четырёх комбинаций, выбор калибратора по фолдам, и во сколько сделок обходится каждый гейт.
Опорные точки для сверки из Global Constraints (полные 30 месяцев, точечные прогоны): дефолтное
положение (`UseDayATRGate=1`, `UseVolume=0`) — 153 сделки, PF 1.822; дневной гейт выключен —
338 сделок, PF 1.348; объёмный гейт включён при `VolMult = 1.2` — 114 сделок, PF 1.765.

- [ ] **Step 3: Записать результат в `_comment` сетки**

Дописать строку вида `РЕЗУЛЬТАТ ПРОГОНА 2026-08-22: pooled OOS PF <...> на <...> сделках, фолды
<...>; гейт дня стоит <...> сделок, объёмный — <...>.` Числа — фактические из отчёта, без
округления в свою пользу.

- [ ] **Step 4: Коммит**

```bash
git add data/params/rsi_pullback/dias/cal_screen.json
git commit -m "feat(rsi_pullback): DIAS, тема screen прогнана"
```

---

### Task 4: Тема `entry` — ключевая, полоса RSI на оборванной оси периода

**Files:**
- Modify: `data/params/rsi_pullback/dias/cal_entry.json`

**Interfaces:**
- Consumes: каталог из Task 1.
- Produces: первое из двух чисел планки (pooled OOS PF темы `entry`, счёт сделок, устойчивость
  `RSILower` по фолдам) — Task 12 выносит по ним вердикт.

- [ ] **Step 1: Прогнать тему**

```bash
go run ./cmd/backtest -ticker DIAS -strategy rsi_pullback -interval Minutes30 \
  -calibrate data/params/rsi_pullback/dias/cal_entry.json -out ./reports/DIAS_entry \
  -months 30 -train-months 10 -test-months 5 -min-trades 20 -metric profit_factor
```

Тема самая тяжёлая: 168 комбинаций × 4 фолда.

- [ ] **Step 2: Выписать числа планки**

Из отчёта: pooled OOS PF, счёт сделок пула, PF и счёт сделок каждого из четырёх фолдов, выбор
`RSIPeriod` / `RSILower` / `RSIUpper` по каждому фолду. Отдельно отметить вырожденные фолды (без
убыточных сделок) — планка их не засчитывает. Сравнить in-sample и OOS по фолдам: разрыв втрое и
больше означает переобучение темы, и это записывается явно (случай IVAT; на SVAV фолд 3 дал 7.893
против 0.568 на 4 сделках, и это назвали прямо).

- [ ] **Step 3: Проверить, не съело ли короткое обучающее окно выборку**

Обучающее окно здесь 10 месяцев, а не 12. Выписать, сколько комбинаций в каждом фолде отсеклось
по `-min-trades 20` (в отчёте это видно по «недобравшим» строкам лидерборда). Если отсев съедает
низкие уровни `RSILower` или высокие `RSIPeriod` систематически — записать это прямо: выбор темы
тогда смещён процедурой, а не данными (эффект, названный числом на ELFV в теме `volume`).

- [ ] **Step 4: Проверить край оси периода**

Ось обрывается на `RSIPeriod = 7` по замеру (RSI(8)@10 даёт 19 кроссов при планке живого угла 29).
Если тема выбирает 7 хотя бы в одном фолде, **перепроверить выбор точечным прогоном `-params`**:
сравнить полноисторичные PF и счёт сделок для 6 и 7 при остальных полях темы. Выбор самого края
оси на десятке сделок — это шум, а не победа периода, и так и записать.

- [ ] **Step 5: Записать результат в `_comment` сетки**

Формат: `РЕЗУЛЬТАТ ПРОГОНА 2026-08-22: pooled OOS PF <...> на <...> сделках, фолды <...> — порог
1.5 <взят|не взят>. Ведущая ось RSILower выбрана <...> — устойчивость <N> из 4.` Плюс отдельные
фразы про отсев по `-min-trades` (Step 3) и край `RSIPeriod = 7` (Step 4).

- [ ] **Step 6: Коммит**

```bash
git add data/params/rsi_pullback/dias/cal_entry.json
git commit -m "feat(rsi_pullback): DIAS, тема entry прогнана"
```

---

### Task 5: Тема `trend` — вторая ключевая

**Files:**
- Modify: `data/params/rsi_pullback/dias/cal_trend.json`

**Interfaces:**
- Consumes: каталог из Task 1.
- Produces: второе число планки (pooled OOS PF темы `trend`, устойчивость `EMASlow` по фолдам).

- [ ] **Step 1: Прогнать тему**

```bash
go run ./cmd/backtest -ticker DIAS -strategy rsi_pullback -interval Minutes30 \
  -calibrate data/params/rsi_pullback/dias/cal_trend.json -out ./reports/DIAS_trend \
  -months 30 -train-months 10 -test-months 5 -min-trades 20 -metric profit_factor
```

- [ ] **Step 2: Выписать числа планки и прочитать их правильно**

pooled OOS PF, счёт сделок, выбор `EMAFast`/`EMASlow` по каждому фолду, устойчивость ведущей оси
`EMASlow`. Ключевая оговорка для DIAS: допуск лежит в 36.7–40.7% на ВСЕХ 35 парах (Global
Constraints), то есть **разница PF между парами не может объясняться размером выборки** — только
качеством фильтра. Если тема гуляет по фолдам (случай ELFV: 200/120/150/50), это означает, что
фильтр направления на инструменте пары не различает, и так и записать.

- [ ] **Step 3: Записать результат в `_comment` сетки**

Формат тот же, что в Task 4 Step 5, с ведущей осью `EMASlow`.

- [ ] **Step 4: Коммит**

```bash
git add data/params/rsi_pullback/dias/cal_trend.json
git commit -m "feat(rsi_pullback): DIAS, тема trend прогнана"
```

---

### Task 6: Темы `day` и `day_spent` — дневной гейт на уплотнённой оси

**Files:**
- Modify: `data/params/rsi_pullback/dias/cal_day.json`, `cal_day_spent.json`

**Interfaces:**
- Consumes: каталог из Task 1, результат темы `screen` (Task 3).
- Produces: выбор `FreshDayATR` и `SpentDayATR` для точки (Task 11).

- [ ] **Step 1: Прогнать обе темы**

```bash
go run ./cmd/backtest -ticker DIAS -strategy rsi_pullback -interval Minutes30 \
  -calibrate data/params/rsi_pullback/dias/cal_day.json -out ./reports/DIAS_day \
  -months 30 -train-months 10 -test-months 5 -min-trades 20 -metric profit_factor

go run ./cmd/backtest -ticker DIAS -strategy rsi_pullback -interval Minutes30 \
  -calibrate data/params/rsi_pullback/dias/cal_day_spent.json -out ./reports/DIAS_day_spent \
  -months 30 -train-months 10 -test-months 5 -min-trades 20 -metric profit_factor
```

- [ ] **Step 2: Сравнить темы прямо**

`cal_day` открывает обе ветки гейта, `cal_day_spent` — только ветку «день исчерпан» при
`FreshDayATR = 0`. Разница pooled PF между ними и есть цена открытой ветки «свежий день». На ELFV
она стоила 0.103 pooled PF в пользу закрытой ветки; выписать это число для DIAS явно.

- [ ] **Step 3: Проверить верхний край 1.5**

Край оставлен по замеру: ветка «день исчерпан» при 1.5 отбирает 9.4% будних баров. Если тема
выбирает 1.5 хотя бы в одном фолде — прогнать точечно `-params` с `SpentDayATR` 1.25, 1.3 и 1.5
поверх прочих дефолтов и выписать счёт сделок и PF. Выбор края на тонкой выборке записывается как
шум, а не как победа края.

- [ ] **Step 4: Записать результаты в `_comment` обеих сеток**

Каждая тема — своя строка `РЕЗУЛЬТАТ ПРОГОНА 2026-08-22: …` с pooled PF, сделками, фолдами и
выбором по фолдам. В `cal_day_spent.json` дополнительно — вывод сравнения из Step 2.

- [ ] **Step 5: Коммит**

```bash
git add data/params/rsi_pullback/dias/cal_day.json data/params/rsi_pullback/dias/cal_day_spent.json
git commit -m "feat(rsi_pullback): DIAS, темы дневного гейта прогнаны"
```

---

### Task 7: Тема `volume` — фон объёмов на суженной оси

**Files:**
- Modify: `data/params/rsi_pullback/dias/cal_volume.json`

**Interfaces:**
- Consumes: каталог из Task 1, результат темы `screen` (Task 3).
- Produces: выбор `UseVolume` / `VolMult` / `VolBaseDays` — Task 11 сверяет его с решёткой Task 8.

- [ ] **Step 1: Прогнать тему**

```bash
go run ./cmd/backtest -ticker DIAS -strategy rsi_pullback -interval Minutes30 \
  -calibrate data/params/rsi_pullback/dias/cal_volume.json -out ./reports/DIAS_volume \
  -months 30 -train-months 10 -test-months 5 -min-trades 20 -metric profit_factor
```

- [ ] **Step 2: Сверить лидерборд с точечными замерами**

Точечные прогоны на полной истории (Global Constraints) дают максимум оси на 1.5 (102 сделки,
PF 1.894) при baseline без гейта 153/1.822. Если тема выбирает другой множитель — назвать
расхождение числом, как это сделали на ELFV: прогнать `-params` для выбора темы и для 1.5 на
полной истории и сравнить PF и счёт сделок. Причина расхождения, если оно есть, обычно
процедурная: `-min-trades 20` на 10-месячном обучающем окне режет высокие множители (при 2.5
остаётся 26 сделок на окно, при 3.0 — 22).

- [ ] **Step 3: Читать pooled PF с осторожностью**

На GAZP и NVTK объёмный гейт покупал pooled PF вырожденным фолдом (фолд без единой убыточной
сделки даёт PF в сотнях и перевешивает пул). Проверить счёт сделок и MaxDD каждого фолда; если
вырожденный фолд есть — записать это прямо, планка его не засчитывает.

- [ ] **Step 4: Записать результат в `_comment` сетки**

`РЕЗУЛЬТАТ ПРОГОНА 2026-08-22: …` с pooled PF, сделками, фолдами, выбором `VolMult`/`VolBaseDays`
по фолдам и выводом Step 2 (совпадает ли выбор темы с максимумом точечных замеров).

- [ ] **Step 5: Коммит**

```bash
git add data/params/rsi_pullback/dias/cal_volume.json
git commit -m "feat(rsi_pullback): DIAS, тема volume прогнана"
```

---

### Task 8: Тема `vol_window` — окно объёмного гейта на расширенной оси

**Files:**
- Modify: `data/params/rsi_pullback/dias/cal_vol_window.json`

**Interfaces:**
- Consumes: каталог из Task 1.
- Produces: выбор `VolLookbackBars` — Task 11 проверяет его в паре с `VolMult` решёткой 3×3.

- [ ] **Step 1: Прогнать тему**

```bash
go run ./cmd/backtest -ticker DIAS -strategy rsi_pullback -interval Minutes30 \
  -calibrate data/params/rsi_pullback/dias/cal_vol_window.json -out ./reports/DIAS_vol_window \
  -months 30 -train-months 10 -test-months 5 -min-trades 20 -metric profit_factor
```

- [ ] **Step 2: Сверить с точечными замерами оси**

Опорные числа (Global Constraints, `VolMult = 2.0`, база 14, полные 30 месяцев): окно 1 → 69
сделок PF 1.294, 2 → 80/1.510, 3 → 88/1.689, 5 → 98/1.867, 8 → 111/1.819, **12 → 121/1.915**,
16 → 126/1.810. Выписать, совпал ли выбор темы с максимумом замера и держится ли выбор по фолдам.

- [ ] **Step 3: Проверить, не эквивалентен ли выбор выключенному гейту**

**Это главная ловушка темы, и она специфична для DIAS.** `VolLookbackBars` расширяет окно, в
котором хотя бы один бар обязан нести повышенный объём, то есть ОСЛАБЛЯЕТ гейт: при окне 16 гейт
пропускает 126 сделок из 153 возможных — почти столько же, сколько выключенный. Если тема выбирает
большое окно (8, 12 или 16), прогнать точечно `-params` три конфигурации на полной истории:
выбранную темой, её же с `UseVolume = 0`, и дефолтную с окном 3. Если первые две дают близкие PF и
счёт сделок — записать, что гейт с таким окном фактически выключен, и **предпочесть в точке
`UseVolume = 0`** как более простую конфигурацию: меньше полей, которые могут разъехаться между
бэктестом и живым раннером.

- [ ] **Step 4: Записать результат в `_comment` сетки**

`РЕЗУЛЬТАТ ПРОГОНА 2026-08-22: …` плюс отдельным предложением — вывод Step 3 (эквивалентен ли
выбор выключенному гейту) и вывод для каталога: стоит ли свипевать `VolLookbackBars` на тикерах с
тонкой ликвидностью. Это второй замер оси в каталоге после ELFV, где максимум стоял ровно на
дефолте ядра, и вывод обязан быть сформулирован так, чтобы им можно было воспользоваться на
следующем тикере.

- [ ] **Step 5: Коммит**

```bash
git add data/params/rsi_pullback/dias/cal_vol_window.json
git commit -m "feat(rsi_pullback): DIAS, тема окна объёмного гейта прогнана"
```

---

### Task 9: Тема `risk` — стоп и цель на оси с возвращённой строкой 0.3

**Files:**
- Modify: `data/params/rsi_pullback/dias/cal_risk.json`

**Interfaces:**
- Consumes: каталог из Task 1.
- Produces: выбор `StopDailyATR` / `TPDailyATR` для точки (Task 11).

- [ ] **Step 1: Прогнать тему**

```bash
go run ./cmd/backtest -ticker DIAS -strategy rsi_pullback -interval Minutes30 \
  -calibrate data/params/rsi_pullback/dias/cal_risk.json -out ./reports/DIAS_risk \
  -months 30 -train-months 10 -test-months 5 -min-trades 20 -metric profit_factor
```

- [ ] **Step 2: Проверить капкан широкого стопа**

Если тема выбирает `StopDailyATR` 1.0 или 1.3, прогнать точечно `-params` всю ось стопа на полной
истории и выписать три ряда: счёт сделок, **долю стоп-выходов** и MaxDD. Капкан выглядит так:
счёт сделок почти не меняется, доля стоп-выходов падает до единиц процентов, MaxDD растёт — стоп
перестал защищать и просто вытесняет убыток в RSI-выход. Так было на WUSH, LENT, LSNGP, IVAT, SVAV
и SIBN. Выживаемость уровней (Global Constraints): 0.7 достаёт 65.6% дней, 1.0 — 36.1%, 1.3 —
17.3%.

- [ ] **Step 3: Отдельно проверить возвращённую строку 0.3**

Строка вернулась в сетку по расчёту издержек (14.1% риска при реальном круге 0.13%), и это первый
случай в каталоге, когда её возвращают. Если тема выбирает 0.3 хотя бы в одном фолде — прогнать
точечно `-params` со стопом 0.3 и с `-commission 0.001` (круг 0.2%, вшестеро выше реального
спреда) и сравнить с тем же прогоном при дефолтной комиссии. Если PF на 0.3 разваливается от
удвоения издержек сильнее, чем на 0.5, — записать это прямо: узкий стоп на этой бумаге живёт
только за счёт дешёвого тика, и в живой торговле запас меньше, чем показывает бэктест.

- [ ] **Step 4: Записать результат в `_comment` сетки**

`РЕЗУЛЬТАТ ПРОГОНА 2026-08-22: …` с pooled PF, сделками, фолдами, выбором стопа и цели по фолдам,
выводами Step 2 и Step 3.

- [ ] **Step 5: Коммит**

```bash
git add data/params/rsi_pullback/dias/cal_risk.json
git commit -m "feat(rsi_pullback): DIAS, тема risk прогнана"
```

---

### Task 10: Темы `exit` и `trail` — выходы

**Files:**
- Modify: `data/params/rsi_pullback/dias/cal_exit.json`, `cal_trail.json`

**Interfaces:**
- Consumes: каталог из Task 1.
- Produces: выбор `RSIUpper`, `UseRSIExit`, `UseTrail`, `TrailDailyATR` для точки (Task 11).

- [ ] **Step 1: Прогнать обе темы**

```bash
go run ./cmd/backtest -ticker DIAS -strategy rsi_pullback -interval Minutes30 \
  -calibrate data/params/rsi_pullback/dias/cal_exit.json -out ./reports/DIAS_exit \
  -months 30 -train-months 10 -test-months 5 -min-trades 20 -metric profit_factor

go run ./cmd/backtest -ticker DIAS -strategy rsi_pullback -interval Minutes30 \
  -calibrate data/params/rsi_pullback/dias/cal_trail.json -out ./reports/DIAS_trail \
  -months 30 -train-months 10 -test-months 5 -min-trades 20 -metric profit_factor
```

- [ ] **Step 2: Разобрать выходы с поправкой на удержание**

Выписать pooled PF и выбор по фолдам для обеих тем. Дополнительно для DIAS: у низких полос
`RSIUpper` сделка становится внутридневной (случай LSNGP, где полоса 55 уронила медиану удержания
до 4 баров), а каждая сделка платит круг издержек. При дешёвом тике (0.0165%) цена этого ниже, чем
на ELFV, но не нулевая — если тема выбирает 55 или 60, выписать медиану удержания и долю
стоп-выходов точечным прогоном и сравнить с полосой 70.

Для темы `trail`: ветка `UseRSIExit = 0` в живой раннер попасть НЕ может —
`TestRegisteredTickersKeepTheRSIExitArmed` держит RSI-выход обязательным для всех тикеров реестра.
Если ветка выигрывает по PF, это записывается как замер, но в точку не берётся; сказать об этом
прямо в `_comment`.

- [ ] **Step 3: Записать результаты в `_comment` обеих сеток**

Каждая тема — своя строка `РЕЗУЛЬТАТ ПРОГОНА 2026-08-22: …`.

- [ ] **Step 4: Коммит**

```bash
git add data/params/rsi_pullback/dias/cal_exit.json data/params/rsi_pullback/dias/cal_trail.json
git commit -m "feat(rsi_pullback): DIAS, темы выходов прогнаны"
```

---

### Task 11: Сборка точки и точечный walk-forward

**Files:**
- Create: `data/params/rsi_pullback/dias/plateau_point.json`

**Interfaces:**
- Consumes: результаты задач 3–10.
- Produces: конкретный набор из восемнадцати полей `core.Params` и его замеры — их прибивает
  Task 12.

- [ ] **Step 1: Собрать точку из лидербордов тем**

Взять по каждой теме её выбор. Где тема мерила ось поверх дефолтов, стоящих вне рабочей зоны
инструмента (случай NVTK и SIBN), — проверить ось точечными прогонами `-params` и записать, что
выбор расходится с темой и почему. На SVAV таких расхождений было три из восьми осей, на SIBN — три
из девяти, на ELFV — три из десяти; это норма, а не сбой.

Наивная сборка «по победителям тем без проверки» ломается предсказуемо: на SVAV она дала 23 сделки
за 36 месяцев при PF 9.18, то есть шум у самой границы стоп-условия. Проверять счёт сделок
собранной точки обязательно.

**Взаимодействие двух объёмных осей.** Если из тем пришли одновременно `VolMult` (Task 7) и
`VolLookbackBars` (Task 8), эти поля взаимодействуют — обе темы мерили свою ось при дефолтном
значении другой. Прогнать точечно решётку 3×3 вокруг обоих выбранных значений и взять пару, а не
двух независимых победителей. Отдельно проверить конфигурацию `UseVolume = 0`: на DIAS гейт даёт
максимум 1.894 против baseline 1.822, то есть выигрыш мал, и выключенный гейт — законный кандидат
в точку, а не запасной вариант.

- [ ] **Step 2: Создать файл точки**

`plateau_point.json` — одна фаза `point`, каждое из восемнадцати полей задано массивом из одного
значения (формат `elfv/plateau_point.json`; массив из двух значений уронит
`TestRSIPullbackPlateauFilesArePoints`, `rsi_pullback_grid_test.go:204`). `_comment` обязан
содержать: замеры принятой точки, явную оговорку «для фиксированной точки это НЕ out-of-sample»,
как собиралась каждая ось (выбор темы или точечный прогон), указание адаптированной схемы
`-months 30 -train-months 10 -test-months 5` с причиной, и команду запуска с путём
`data/params/rsi_pullback/dias/plateau_point.json`.

Поля, которые обязаны быть в файле: `RSIPeriod`, `RSILower`, `RSIUpper`, `EMAFast`, `EMASlow`,
`DailyATRPeriod`, `UseDayATRGate`, `FreshDayATR`, `SpentDayATR`, `StopDailyATR`, `TPDailyATR`,
`UseVolume`, `VolBaseDays`, `VolLookbackBars`, `VolMult`, `UseRSIExit`, `UseTrail`, `TrailDailyATR`.

Отдельно записать, какие поля НИ ОДНОЙ темой не свипуются и остаются дефолтом ядра, а не выбором.
На DIAS, как и на ELFV, это **только `DailyATRPeriod`**: `VolLookbackBars` измерен темой
`vol_window`.

- [ ] **Step 3: Прогнать точку тем же walk-forward**

```bash
go run ./cmd/backtest -ticker DIAS -strategy rsi_pullback -interval Minutes30 \
  -calibrate data/params/rsi_pullback/dias/plateau_point.json -out ./reports/DIAS_point \
  -months 30 -train-months 10 -test-months 5 -min-trades 20 -metric profit_factor
```

- [ ] **Step 4: Проверить стоп-условие плана**

Если pooled OOS PF < 1.0 или сделок меньше 20 — остановиться, вынести числа владельцу, задачи 12–15
не выполнять до его решения. С учётом априора (baseline PF 1.822 на 153 сделках) сработать оно не
должно, но проверка обязательна.

- [ ] **Step 5: Замерить плато соседями**

По каждой оси прогнать соседние значения точечно и выписать pooled PF и счёт сделок: плато шириной
в один шаг — это пик, а не полка, и в доке пакета это должно быть названо (случай UGLD, где
`RSILower` 20 роняет точку с 3.627 до 1.580).

- [ ] **Step 6: Проверить, что результат не держится одной неделей**

Прогнать принятую точку через `-params` на OOS-окне и разложить итог: вклад лучшей недели, лучшего
месяца, топ-1 и топ-5 сделок, распределение по полугодиям, число убыточных месяцев. На IVAT 85%
результата сделала одна неделя июля 2026, и без этой проверки pooled PF читается неверно; на SVAV,
SIBN и ELFV та же проверка показала обратное (9.2%, 17.9%, 28.3%).

**Поправка для DIAS:** все шесть полугодий истории падающие, поэтому «прибыльны все полугодия»
здесь означает больше, чем на других тикерах, — выписать разбивку по полугодиям обязательно.

- [ ] **Step 7: Замерить точку под удвоенными издержками**

Прогнать принятую точку тем же walk-forward с `-commission 0.001` (круг 0.2%) и выписать pooled
OOS PF и счёт сделок рядом с дефолтными. Это не критерий приёма — планка считается по дефолтной
комиссии, как у всего каталога, — но число обязано быть записано. Опорная точка: на дефолтах
удвоение круга роняет PF 1.822 → 1.567 (−14%), тогда как на ELFV та же операция стоила −18%.
Круг 0.2% здесь ВШЕСТЕРО выше реального спреда (шаг цены 0.0165%), то есть это заведомо
пессимистичная граница, а не ожидание.

- [ ] **Step 8: Коммит**

```bash
git add data/params/rsi_pullback/dias/plateau_point.json
git commit -m "feat(rsi_pullback): DIAS, принятая точка и её замеры"
```

---

### Task 12: Литерал в пакете и снимок

**Files:**
- Modify: `internal/service/trading_strategy/rsi_pullback/strategy/dias/dias.go`
- Modify: `internal/service/trading_strategy/rsi_pullback/strategy/dias/dias_test.go`
- Modify: `internal/service/backtest/rsi_pullback_registry_test.go` (заменить baseline-тест снимком)

**Interfaces:**
- Consumes: набор полей из Task 11.
- Produces: `dias.DefaultParams()`, возвращающий литерал, — его читают Task 13 (реестр раннера) и
  Task 14 (вселенная).

- [ ] **Step 1: Заменить тест baseline снимком литерала**

В `dias_test.go` удалить `TestParamsTrackTheBaseline` и написать снимок по образцу `elfv_test.go`:
`TestCalibratedLiteralIsPinned` (все восемнадцать полей), `TestParamsDoNotTrackTheBaseline`,
`TestTickerIsDIAS`, `TestStopIsArmed`, `TestRSIExitIsArmed`, плюс тесты под фактически принятую
конфигурацию — `TestOnlySpentDayBranchIsArmed` / `TestVolumeGateIsArmed` / `TestTrailStaysOff`
пишутся под то, что получилось, а не копируются вслепую. Каждый тест несёт в комментарии замер,
объясняющий, почему поле именно такое.

Обязательные инварианты, которые снимок обязан сторожить независимо от результата калибровки:
`StopDailyATR > 0`, `UseRSIExit == 1` (живой раннер держит RSI-выход обязательным для всех тикеров
реестра — это проверяет `TestRegisteredTickersKeepTheRSIExitArmed`), `RSIUpper > RSILower`,
`TPDailyATR > 0`, и при `UseTrail == 0` — `TrailDailyATR == 0`.

**Дополнительный тест, специфичный для DIAS:** `TestStopStaysAboveTheCostFloor` — проверяет
`StopDailyATR >= 0.3`. Комментарий несёт замер: шаг цены 0.5 ₽ = 0.0165% медианной цены 3033.5,
реальный круг издержек ≈ 0.13%, и на стопе 0.3 ATR (0.92% цены) это 14.1% риска — под чертой 17%,
по которой строку 0.3 вырезали из `domrf/` и `elfv/`. Уже 0.3 ATR на этом инструменте стоп
перестаёт окупать издержки, и литерал не имеет права оказаться уже него через правку руками.

- [ ] **Step 2: Запустить и убедиться, что падает**

Run: `go test ./internal/service/trading_strategy/rsi_pullback/strategy/dias/ -v`
Expected: FAIL — `DefaultParams()` ещё возвращает baseline.

- [ ] **Step 3: Поставить литерал**

В `dias.go` заменить `return core.DefaultParams()` литералом из Task 11 и переписать доку пакета:
раздел «СОСТОЯНИЕ: калибровка не проводилась» → разбор калибровки (результат десяти тем, вердикт по
планке пункт за пунктом, разбор каждого поля литерала, граница приёма). Замеры инструмента из
прежней редакции доки сохраняются целиком — включая априор, битый кэш, шаг цены, ликвидность, дыры
в барах и режим, потому что они не устаревают от прогонов.

Отдельными абзацами:

1. **что дала тема `vol_window`** — выбрала ли она окно, отличное от дефолтного 3, оказался ли
   выбор эквивалентен выключенному гейту, и что из этого следует для следующих тикеров с тонкой
   ликвидностью. Это второй замер оси в каталоге, и он спорит с выводом ELFV;
2. **как читать результат при режиме без единого растущего полугодия** — что именно проверено
   падением и чего проверить было нельзя.

- [ ] **Step 4: Заменить сторожевой тест в реестре бэктеста**

`TestRSIPullbackDIASTracksBaseline` → `TestRSIPullbackDIASIsRegisteredAndCalibrated` по образцу
теста ELFV: проверяет наличие в карте, несовпадение с baseline, равенство литералу пакета и
`Ticker()`. Комментарий теста обязан назвать вердикт по планке — взята она или нет, с числами
обеих ключевых тем и с указанием адаптированной схемы прогонов.

- [ ] **Step 5: Запустить тесты и линт**

Run: `go test ./internal/service/trading_strategy/rsi_pullback/... ./internal/service/backtest/ && ./bin/golangci-lint run ./internal/service/...`
Expected: PASS, 0 issues.

- [ ] **Step 6: Коммит**

```bash
git add internal/service/trading_strategy/rsi_pullback/strategy/dias internal/service/backtest/rsi_pullback_registry_test.go
git commit -m "feat(rsi_pullback): DIAS откалиброван — литерал вместо отслеживания baseline"
```

---

### Task 13: Реестр живого раннера

**Files:**
- Modify: `internal/service/trading_strategy/rsi_pullback/live/registry.go`

**Interfaces:**
- Consumes: `dias.Ticker`, `dias.DefaultParams()` из Task 12.
- Produces: `ParamsFor("DIAS")` и `StrategyFor("DIAS")`, без которых раннер тикер не увидит.

- [ ] **Step 1: Добавить импорт и запись в карту**

Импорт `"tinvest/internal/service/trading_strategy/rsi_pullback/strategy/dias"` рядом с остальными
(образец — строка 6, импорт `elfv`) и строка карты рядом с записью ELFV (строка 175):

```go
	dias.Ticker:  dias.DefaultParams(),
```

- [ ] **Step 2: Дописать комментарий карты**

Абзац про DIAS: адаптированная схема прогонов 30/10/5 с причиной (истории 30.3 месяца, IPO
2024-02-13), вердикт по планке с числами обеих ключевых тем, априор (baseline PF 1.822 на 153
сделках, holdout скринера 3.35) и чем он закончился, режим без единого растущего полугодия
(−79.0% за окно) как главная режимная оговорка, тонкая ликвидность (42.6 млн ₽ медианой, p10 13.6)
как ограничение исполнения, дешёвый шаг цены 0.5 ₽ = 0.0165% как единственный параметр
исполнения, который у этого тикера ЛУЧШЕ, чем у ELFV, и починенный кэш как процедурный урок.

- [ ] **Step 3: Запустить тесты пакета**

Run: `go test ./internal/service/trading_strategy/rsi_pullback/live/ -v`
Expected: PASS — включая `TestRegisteredTickersKeepTheRSIExitArmed`, который обходит всю карту, и
`TestBaselineTrackingTickersStayOutOfTheDefaultUniverse`.

- [ ] **Step 4: Коммит**

```bash
git add internal/service/trading_strategy/rsi_pullback/live/registry.go
git commit -m "feat(rsi_pullback): DIAS в реестре живого раннера"
```

---

### Task 14: Заведение в боевую вселенную

**Files:**
- Modify: `internal/config/rsi_pullback.go` (дефолт `Tickers` на строке 150 + комментарий)
- Modify: `internal/config/rsi_pullback_test.go:54` (ожидание дефолта)
- Modify: `env/prod.env:20`, `env/prod.env.example:30`, `env/local.env.example:27`
- Modify: `docs/rsi_pullback/live.md` (таблица §8 строка 207, раздел про реестр строка 250, §9
  порядок выката строка 294)

**Interfaces:**
- Consumes: литерал из Task 12, запись реестра из Task 13.
- Produces: боевую вселенную из пятнадцати тикеров.

- [ ] **Step 1: Проверить стоп-условие**

Если Task 11 остановился на стоп-условии — эта задача не выполняется. Иначе продолжать.

- [ ] **Step 2: Обновить тест дефолта**

```go
	want := []string{"UGLD", "T", "GAZP", "DOMRF", "FESH", "WUSH", "LENT", "RENI", "NVTK", "LSNGP", "IVAT", "SVAV", "SIBN", "ELFV", "DIAS"}
```

- [ ] **Step 3: Запустить тест и убедиться, что падает**

Run: `go test ./internal/config/ -run TestNewRSIPullbackConfig_Defaults -v`
Expected: FAIL — дефолт ещё из четырнадцати тикеров.

- [ ] **Step 4: Обновить дефолт и env-файлы**

`Tickers: []string{"UGLD", "T", "GAZP", "DOMRF", "FESH", "WUSH", "LENT", "RENI", "NVTK", "LSNGP", "IVAT", "SVAV", "SIBN", "ELFV", "DIAS"}`
и та же строка в `RSI_PULLBACK_TICKERS` трёх env-файлов. В комментарий функции дописать абзац про
DIAS с типом его риска: **риск здесь РЕЖИМНЫЙ и статистический, а не исполнительный** — тикер
единственный во вселенной, у которого за всю историю нет ни одного растущего полугодия, поэтому
против роста конфигурация не проверялась вовсе; история 30 месяцев и укороченные окна 10/5 делают
числа несопоставимыми построчно с каталогом; ликвидность тонкая (42.6 млн ₽ медианой). Отдельной
фразой — что шаг цены здесь дешёвый (0.0165% против 0.039% у ELFV), то есть исполнительный риск у
этого тикера НИЖЕ, чем у соседа по вселенной.

- [ ] **Step 5: Обновить live.md**

Таблица §8 (дефолт переменной, строка 207), раздел про реестр («знает четырнадцать пакетов»,
строка 250 — заменить на пятнадцать и добавить `dias` в перечисление), §9 пункт 1 — добавить DIAS
в список сверки `pullparity` (строка 294).

- [ ] **Step 6: Запустить тесты**

Run: `go test ./internal/config/ ./internal/service/trading_strategy/rsi_pullback/... -v`
Expected: PASS — `TestEveryDefaultTickerIsRegistered` и
`TestBaselineTrackingTickersStayOutOfTheDefaultUniverse` читают вселенную из конфига и покроют
новый состав автоматически.

- [ ] **Step 7: Коммит**

```bash
git add internal/config env docs/rsi_pullback/live.md
git commit -m "feat(rsi_pullback): завести DIAS в боевую вселенную"
```

---

### Task 15: Документация — разбор калибровки и принятый риск

**Files:**
- Modify: `docs/rsi_pullback/strategy.md` (§8.0.1, ячейка «откалиброван» на строке 340 + раздел с
  разбором прогонов в конце файла, после раздела ELFV на строке 948)
- Modify: `docs/rsi_pullback/live.md` (§10, риск 18 — следующий за риском 17 про ELFV, строка 697)

**Interfaces:**
- Consumes: числа задач 3–11, решение задачи 14.
- Produces: справочник, по которому тикер сопровождают в живой торговле.

- [ ] **Step 1: Дописать §8.0.1 `strategy.md`**

В ячейку «откалиброван» добавить `dias` с датой, схемой прогонов (АДАПТИРОВАННАЯ 30/10/5 с
причиной), вердиктом по планке, замерами принятой точки и ссылкой на риск 18 в `live.md`. Отдельно
назвать три вещи, которых в каталоге не было:

1. **режим без единого растущего полугодия** — первый такой тикер;
2. **битый кэш**, найденный до калибровки, и процедурное следствие для следующих тикеров;
3. **второй замер оси `VolLookbackBars`**, спорящий с выводом ELFV.

- [ ] **Step 2: Дописать раздел с разбором прогонов**

По образцу разделов ELFV, SIBN и SVAV: рамки данных, режим, вердикт по планке пункт за пунктом,
разбор каждого поля литерала, граница приёма («для фиксированной точки это НЕ out-of-sample»).
Отдельными абзацами — четыре свойства, специфичных для этого тикера:

1. **режим −79.0% без единого растущего полугодия** — что проверено падением и что проверить было
   нельзя;
2. **адаптированная схема 30/10/5** — почему четыре фолда важнее штатных размеров окон и с чем
   теперь сопоставимы числа;
3. **тема `vol_window` во второй раз** — что дал замер оси на тонкой бумаге и как это соотносится
   с выводом ELFV («на других тикерах тему не заводить»);
4. **дешёвый шаг цены** — почему строка стопа 0.3 вернулась в сетку и чем это отличает DIAS от
   ELFV и DOMRF.

- [ ] **Step 3: Дописать риск 18 в `live.md` §10**

Замеры, практические следствия для наблюдения (распределение выходов, медиана удержания,
просадка), и четыре ограничения:

1. **против роста конфигурация не проверялась вовсе**: все шесть полугодий истории падающие
   (−6.7 / −26.7 / −18.5 / −38.2 / −4.2 / −29.4%), окно −79.0%. Это зеркало риска 13 (LSNGP), у
   которого не было падающего окна. Первое растущее полугодие — настоящее испытание точки;
2. **история 30 месяцев и укороченные окна 10/5**: числа сопоставимы с IVAT, но не построчно с
   остальным каталогом; обучающее окно чувствительнее к локальному режиму, чем двенадцатимесячное;
3. **ликвидность тонкая**: медиана 42.6 млн ₽/день, p10 13.6 млн; гейт отбора скринера в 50 млн
   тикер проходит только средним; размер живой позиции соотносить с p10, а не с медианой;
4. **дыры в барах**: 11.9% будних дней несут меньше двадцати баров, 4.1% баров имеют нулевой
   размах; RSI считается по неравномерно расставленным барам, и ни один параметр этого не
   устраняет.

Плюс две отдельные строки:

- **про риск, который здесь НИЖЕ, чем у соседей**: шаг цены 0.5 ₽ = 0.0165% медианной цены, втрое
  дешевле ELFV; удвоение круга издержек стоит −14% PF против −18% у ELFV. Исполнительный риск на
  этом тикере — не главный;
- **про дивидендную отсечку**: два гэпа хуже −3% за 30 месяцев (худший −5.67%, 2024-06-13);
  стратегия держит позицию через ночь, стоп такой гэп не защищает.

- [ ] **Step 4: Проверить, что доки не противоречат числам**

Run: `grep -rn "DIAS" docs/rsi_pullback/*.md internal/service/trading_strategy/rsi_pullback/strategy/dias/*.go internal/config/rsi_pullback.go`
Сверить каждое число с отчётами прогонов в `./reports/DIAS_*`.

- [ ] **Step 5: Коммит**

```bash
git add docs/rsi_pullback
git commit -m "docs(rsi_pullback): разбор калибровки DIAS и принятый риск"
```

---

### Task 16: Финальная проверка

**Files:** нет изменений, только проверки.

- [ ] **Step 1: Полный гейт качества**

Run: `./bin/mage ci`
Expected: lint + `go test -race ./...` + проверка дрейфа моков — зелёные.

- [ ] **Step 2: Сверка живой сборки с бэктестом**

```bash
go run ./cmd/pullparity -tickers DIAS -months 24
```
Expected: ноль расхождений. **24 месяца, а не 30:** живой сборщик тянет дневные свечи окном
`dailyFetchDays = 730` (`live/marketdata/marketdata.go:47`), и на большем горизонте появляются
ожидаемые расхождения длины `Daily*` рядов (`maxDailyHorizonMonths`, выяснено на IVAT).
Расхождение на 24 месяцах означает, что живой раннер и бэктест считают сигнал по-разному, и
заведение в прод откатывается до выяснения.

- [ ] **Step 3: Записать результат сверки в `live.md` §9**

Строка вида «DIAS заведён 2026-08-22 и сверяется за 24 месяца (`go run ./cmd/pullparity -tickers
DIAS -months 24` — <N> баров, **ноль расхождений**)» рядом с такими же строками про IVAT, SVAV,
SIBN и ELFV. Коммит: `docs(rsi_pullback): сверка DIAS — 24 месяца, ноль расхождений`.

- [ ] **Step 4: Сообщить владельцу результат**

Короткий отчёт: вердикт по планке пункт за пунктом, замеры принятой точки (включая её результат под
удвоенными издержками и разбивку по полугодиям), что заведено в прод, какие риски записаны, что
осталось (первые живые сделки, условия пересмотра). Тремя отдельными строками:

1. **чем закончилась тема `vol_window` во второй раз** — подтверждается ли вывод ELFV («дефолт
   ядра 3 оптимален, тему не заводить») или опровергается, и по какому признаку выбирать тикеры,
   на которых ось стоит свипевать;
2. **что дал режим без единого растущего полугодия** — насколько результат лонговой стратегии на
   падающей на 79% бумаге меняет доверие к остальному каталогу, где режим был мягче;
3. **процедурный урок про битый кэш** — предложение проверять непрерывность обеих серий перед
   калибровкой каждого следующего тикера.

---

## Self-review

**Покрытие спеки.** Кэш и его починка → Global Constraints, дока пакета (Task 2 Step 3), Task 15
Step 1 пункт 2, Task 16 Step 4 пункт 3; схема 30/10/5 → Global Constraints и каждая команда
прогона, дока пакета, Task 13 Step 2, Task 15 Step 2 пункт 2; априор → Global Constraints, дока
пакета, Task 12 Step 3, Task 13 Step 2; свойство 1 (режим) → Global Constraints, Task 11 Step 6,
Task 14 Step 4, Task 15 Step 2 пункт 1 и Step 3 пункт 1; свойство 2 (шаг цены) → Task 1 (тест
возвращённой строки 0.3), Task 9 Step 3, Task 11 Step 7, Task 12 Step 1
(`TestStopStaysAboveTheCostFloor`), Task 15 Step 3; свойство 3 (ликвидность и дыры в барах) → дока
пакета, тема `vol_window` (Task 1 + Task 8), Task 15 Step 3 пункты 3 и 4; свойство 4 (гэпы) → дока
пакета, Task 15 Step 3; свойство 5 (трендовый допуск) → сторожевой тест Task 1, Task 5 Step 2;
свойство 6 (ATR и выживаемость стопов) → Task 1, Task 9 Step 2; свойство 7 (объёмный гейт и его
окно) → Task 1, Task 7, Task 8, Task 11 Step 1; свойство 8 (дневной гейт) → Task 1, Task 3 Step 2,
Task 6; свойство 9 (полоса выхода) → Task 1, Task 10; оси десяти сеток → Task 1 Step 3; обрыв
`RSIPeriod` на 7 → Task 1 (сетка + два сторожевых утверждения) и Task 4 Step 4; сохранение края
дневного гейта 1.5 → Task 1 и Task 6 Step 3; сужение `VolMult` до 2.5 → Task 1 и Task 7 Step 2;
расширение `VolLookbackBars` до 16 → Task 1 и Task 8; возврат строки стопа 0.3 → Task 1 и Task 9
Step 3; планка → Global Constraints, вердикт выносится в Task 12 и Task 15; правило прода и
стоп-условие → Task 11 Step 4 и Task 14 Step 1; артефакты спеки → задачи 1, 2, 12, 13, 14, 15;
порядок работы спеки → порядок задач; риск 5 спеки (расширенная ось ослабляет гейт) → Task 8
Step 3 и Task 11 Step 1.

**Плейсхолдеры.** В задачах 3–11 значения результатов прогонов не выдуманы: там, где число станет
известно только после запуска, стоит явное указание выписать его из отчёта — это данные, которых до
прогона не существует, а не плейсхолдер плана. Код сторожевого теста осей, теста baseline, доки
пакета и все десять сеток даны целиком. Снимок литерала (Task 12) задан списком обязательных
инвариантов плюс одним конкретным тестом (`TestStopStaysAboveTheCostFloor` с порогом 0.3), потому
что остальные его значения — результат Task 11.

**Согласованность типов.** `dias.Ticker` (строка `"DIAS"`) и `dias.DefaultParams() core.Params`
объявлены в Task 2 и используются под теми же именами в задачах 12, 13, 14. Хелпер `diasGrid`
объявлен в Task 1; `containsValue`, `sameSet` и `rsiPullbackTickerGrid` берутся из существующих
файлов пакета и НЕ переобъявляются (это отдельно оговорено в Task 1 — иначе пакет не соберётся).
Имена тестов, заменяемых на следующем шаге (`TestParamsTrackTheBaseline` → снимок,
`TestRSIPullbackDIASTracksBaseline` → `TestRSIPullbackDIASIsRegisteredAndCalibrated`), названы в
обеих задачах одинаково. Имя фазы `vol_window` совпадает в сетке (Task 1 Step 3), в команде
прогона (Task 8 Step 1) и в ссылках задач 11, 12, 15. Число тикеров вселенной (пятнадцать)
согласовано между Task 13 Step 2, Task 14 Step 2/4/5 и Task 15.
