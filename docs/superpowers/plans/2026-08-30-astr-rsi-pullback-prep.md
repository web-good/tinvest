# ASTR под `rsi_pullback` — план подготовки, калибровки и вердикта

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Довести ASTR (Группа Астра) до вердикта по стратегии `rsi_pullback`: каталог максимально
широких сеток, тематический walk-forward, принятая точка, литерал в пакете и заведение в боевую
вселенную девятнадцатым тикером.

**Architecture:** Процедура тем **каноническая**: все десять тем идут поверх дефолтов ядра,
поэтому каталог сеток создаётся целиком одной задачей, якоря нет, поздних файлов нет. Два
отступления, и оба в данных, а не в процедуре: расчётное окно — 34 месяца с IPO бумаги, схема
прогонов адаптированная **34/10/6**; сетки держатся **максимально широкими** по решению владельца,
поэтому сторожевой тест осей запрещает урезать ось, а не расширять её.

**Tech Stack:** Go 1.25, `cmd/backtest` (rolling walk-forward), `cmd/pullparity` (сверка живой
сборки с бэктестом), `./bin/mage ci` (lint + `go test -race ./...` + дрейф моков).

**Spec:** `docs/superpowers/specs/2026-08-30-astr-rsi-pullback-prep-design.md`

## Global Constraints

- **Схема прогонов — адаптированная:** `-months 34 -train-months 10 -test-months 6 -min-trades 20
  -metric profit_factor`, четыре фолда встык (10 + 6×4 = 34). У темы `screen` — `-min-trades 1`.
  Пропуск любого из трёх флагов окна даёт другую схему и несравнимые числа. Расчётное окно —
  **2023-10-29 … 2026-08-29**.
- **`-refresh` НЕ запускать ни на одном шаге.** Кэш перезалит 2026-08-29 до первого замера:
  `ASTR_Minutes30.json` — 34 551 бар (2023-10-13 … 2026-08-28), в окне 34 353, из них 24 287
  будних; `ASTR_Day1.json` — 848 свечей (2023-10-13 … 2026-08-27), в окне 837, из них 719 будних.
  Запас истории до левой границы окна — 16 дней, на прогрев дневного ATR(14) хватает впритык. Любой
  refresh сдвинет обе границы и сделает все числа спеки несравнимыми.
- **Сетки держатся максимально широкими** (решение владельца 2026-08-30, то же, что по BANEP
  2026-08-28). Обрезок осей не делается; замеры, которые в узком каталоге были бы основанием
  вырезать край, идут в `_comment` как **предупреждения** о том, чего ждать на краю. Жёстких
  инвариантов три: `RSILower` ≤ 50, `RSIPeriod` ≥ 3, ось тренда не порождает пар
  `EMAFast ≥ EMASlow`.
- **Цена широкой оси названа заранее:** она повышает риск переоптимизации на обучающем окне,
  поэтому пофолдовая устойчивость читается строже — край, победивший в одном фолде и мёртвый в
  остальных, в точку не идёт. Правило сборки точки: **не менее трёх фолдов из четырёх** за одно
  значение оси, иначе ось остаётся на дефолте ядра.
- **Планка** (объявлена до прогонов, не пересматривается): темы `entry` и `trend` обе дают pooled
  OOS PF ≥ 1.5 при ≥ 20 сделках в пуле; ведущая ось (`RSILower` для `entry`, `EMASlow` для `trend`)
  выбрана одинаково в ≥ 3 фолдах из 4; вырожденный фолд (ни одной убыточной сделки) в пользу
  тикера не засчитывается; счёт по дефолтной комиссии (круг 0.1%).
- **Правило прода:** литерал ставится и ASTR заводится в `RSI_PULLBACK_TICKERS` девятнадцатым
  **независимо от того, взята планка или нет**.
- **Стоп-условие:** работа останавливается, если принятая точка даёт pooled OOS PF < 1.0, **либо**
  меньше 20 сделок за расчётное окно, **либо** PF < 1.0 под удвоенными издержками
  (`-commission 0.001`). При срабатывании — числа владельцу, задачи 12–15 не выполняются.
- **Каждый `_comment` сетки** обязан содержать: что тема меряет и сколько в ней прогонов; замер, из
  которого получена каждая ось, с предупреждением о крае; полную команду запуска с путём
  `data/params/rsi_pullback/astr/<файл>` (этого требует `TestRSIPullbackCalFilesValid`); место под
  строку `РЕЗУЛЬТАТ ПРОГОНА 2026-08-30: …`.
- **Коммит по завершении каждой задачи**, сообщения на русском в стиле существующей истории.
- **Дефолты ядра** (`core.DefaultParams()`), поверх которых считают ВСЕ темы: `RSIPeriod 4`,
  `RSILower 30`, `RSIUpper 70`, `EMAFast 10`, `EMASlow 100`, `DailyATRPeriod 14`,
  `UseDayATRGate 1`, `FreshDayATR 0`, `SpentDayATR 0.8`, `StopDailyATR 0.5`, `TPDailyATR 0.6`,
  `UseVolume 0`, `VolBaseDays 14`, `VolLookbackBars 3`, `VolMult 1.2`, `UseRSIExit 1`,
  `UseTrail 0`, `TrailDailyATR 0`. Контрольный прогон дефолтов на расчётном окне: **158 сделок,
  PF 1.093, net +7278 ₽** — предпоследнее место в ряду baseline каталога.
- **Полугодия расчётного окна** (нужны в Task 11 Step 7): 2023-10-30 … 2024-04-18 (+17.7%),
  2024-04-18 … 2024-10-07 (−8.0%), 2024-10-08 … 2025-03-28 (−24.2%), 2025-03-31 … 2025-09-17
  (−7.1%), 2025-09-18 … 2026-03-09 (−34.7%), 2026-03-09 … 2026-08-28 (−15.5%).
- **Ветка:** `feat/astr-pullback-prep` (создана от `main` `ffbb7f9`, спека закоммичена `fb40576`).

---

### Task 1: Каталог десяти сеток со сторожевым тестом осей

**Files:**
- Create: `data/params/rsi_pullback/astr/cal_screen.json`
- Create: `data/params/rsi_pullback/astr/cal_entry.json`
- Create: `data/params/rsi_pullback/astr/cal_trend.json`
- Create: `data/params/rsi_pullback/astr/cal_day.json`
- Create: `data/params/rsi_pullback/astr/cal_day_spent.json`
- Create: `data/params/rsi_pullback/astr/cal_volume.json`
- Create: `data/params/rsi_pullback/astr/cal_vol_window.json`
- Create: `data/params/rsi_pullback/astr/cal_risk.json`
- Create: `data/params/rsi_pullback/astr/cal_exit.json`
- Create: `data/params/rsi_pullback/astr/cal_trail.json`
- Create: `internal/service/backtest/rsi_pullback_astr_grid_test.go`

**Interfaces:**
- Consumes: хелперы пакета `backtest` — `rsiPullbackTickerGrid(t, ticker, file)`
  (`rsi_pullback_grid_test.go:40`), `sameSet` (`rsi_pullback_reni_grid_test.go:17`), `containsValue`
  (`rsi_pullback_ivat_grid_test.go:88`); объявлять их заново НЕЛЬЗЯ — пакет один, будет
  переопределение. Общие тесты `TestRSIPullbackCalFilesValid` (`rsi_pullback_grid_test.go:89`) и
  `TestRSIPullbackGridControlPoints` (там же, :147) подхватывают новый каталог сами.
- Produces: каталог `data/params/rsi_pullback/astr/` и функцию `astrGrid(t, file)` — ею пользуются
  только тесты этого файла.

- [ ] **Step 1: Написать падающий сторожевой тест осей**

Создать `internal/service/backtest/rsi_pullback_astr_grid_test.go`:

```go
package backtest

import "testing"

// astrGrid читает файл сеток ASTR через общий хелпер.
func astrGrid(t *testing.T, file string) map[string][]float64 {
	t.Helper()
	return rsiPullbackTickerGrid(t, "astr", file)
}

// TestASTRGridsStayWide сторожит оси всех десяти тем каталога astr/. Каталог собран 2026-08-30 по
// замерам самого ASTR на расчётном окне 2023-10-29 … 2026-08-29 (34 551 получасовой бар в кэше, в
// окне 34 353, из них 24 287 будних; дневная серия 848 свечей, в окне 719 будних).
//
// РЕШЕНИЕ ВЛАДЕЛЬЦА 2026-08-30: сетки этого тикера держатся МАКСИМАЛЬНО ШИРОКИМИ, как у BANEP.
// Поэтому тест сторожит оси с ДРУГОЙ стороны, чем у большинства тикеров каталога: он проверяет,
// что край оси не урезали, а не что его не расширили. Замеры, которые в узком каталоге были бы
// основанием обрезать край, живут в _comment каждой сетки как предупреждения о том, чего ждать.
//
// Инвариантов, которые остаются жёсткими, всего три, и все три — про смысл, а не про диапазон:
//
//   - RSILower не может быть выше 50: 50 — средняя линия осциллятора, выше неё отката нет по
//     определению, там кросс вниз означает уже не откат, а начало движения.
//   - RSIPeriod не может быть короче 3: двойка — это не откат, а тик.
//   - Ось тренда не может порождать пары EMAFast >= EMASlow: при равенстве фильтр вырождается,
//     при инверсии становится другим фильтром («медленная над быстрой»).
//
// ОКНО КОРОЧЕ КАТАЛОЖНОГО: первый бар получасовой серии 2023-10-13 (IPO Группы Астра), доступно
// 34.5 месяца, канон 36/12/6 не помещается. Схема адаптирована до 34/10/6 — четыре фолда встык,
// жертвуется длина обучающего окна, а не число фолдов, потому что правило сборки точки «≥3 фолда
// из 4» при трёх фолдах вырождается. Прецеденты: IVAT 25/9/4, DIAS 30/10/5, YDEX 25/9/4.
func TestASTRGridsStayWide(t *testing.T) {
	screen := astrGrid(t, "cal_screen.json")
	for _, field := range []string{"UseDayATRGate", "UseVolume"} {
		if got := screen[field]; !sameSet(got, 0, 1) {
			t.Errorf("cal_screen.json: %s = %v, want ровно {0,1} — тема меряет цену каждого гейта в сделках", field, got)
		}
	}

	entry := astrGrid(t, "cal_entry.json")
	for _, v := range entry["RSILower"] {
		if v > 50 {
			t.Errorf("cal_entry.json свипует RSILower=%v: выше 50 отката нет по определению", v)
		}
	}
	for _, v := range entry["RSIPeriod"] {
		if v < 3 {
			t.Errorf("cal_entry.json свипует RSIPeriod=%v: короче тройки — уже не откат, а тик", v)
		}
	}
	// Ось входа держится широкой по обеим границам. На ASTR рельеф уровня монотонный и сильный
	// (RSI(4): 10 -> 2.252, 20 -> 1.714, 30 -> 1.093, 45 -> 0.987), поэтому глубокий край 10
	// обязан остаться в сетке, а мелкий край 50 — чтобы монотонность была видна целиком.
	for _, v := range []float64{3, 8} {
		if !containsValue(entry["RSIPeriod"], v) {
			t.Errorf("cal_entry.json: RSIPeriod = %v, не содержит %v — ось входа держится широкой по решению владельца", entry["RSIPeriod"], v)
		}
	}
	for _, v := range []float64{10, 50} {
		if !containsValue(entry["RSILower"], v) {
			t.Errorf("cal_entry.json: RSILower = %v, не содержит %v — оба края оси обязаны остаться", entry["RSILower"], v)
		}
	}
	for _, v := range []float64{55, 85} {
		if !containsValue(entry["RSIUpper"], v) {
			t.Errorf("cal_entry.json: RSIUpper = %v, не содержит %v — полоса выхода меряется целиком", entry["RSIUpper"], v)
		}
	}

	trend := astrGrid(t, "cal_trend.json")
	// Ось тренда на ASTR шумит без монотонности (5/30 -> 1.182, 10/30 -> 0.952, 20/50 -> 1.135,
	// 20/100 -> 0.938, 40/200 -> 1.069), края не выделяются — тем более она меряется целиком.
	for _, v := range []float64{30, 200} {
		if !containsValue(trend["EMASlow"], v) {
			t.Errorf("cal_trend.json: EMASlow = %v, не содержит %v — ось тренда меряется целиком", trend["EMASlow"], v)
		}
	}
	for _, f := range trend["EMAFast"] {
		for _, s := range trend["EMASlow"] {
			if f >= s {
				t.Errorf("cal_trend.json порождает пару EMAFast=%v >= EMASlow=%v: фильтр вырожден или инвертирован", f, s)
			}
		}
	}

	day := astrGrid(t, "cal_day.json")
	// Ноль в свежей ветке обязателен: на всех прод-тикерах каталога победил именно он, а на ASTR
	// свежая ветка разбавляет (0.2 -> 185 сделок/1.053 против 158/1.093 при нуле).
	if !containsValue(day["FreshDayATR"], 0) {
		t.Errorf("cal_day.json: FreshDayATR = %v, не содержит 0 — на всех прод-тикерах каталога победил ноль", day["FreshDayATR"])
	}
	for _, name := range []string{"cal_day.json", "cal_day_spent.json"} {
		spent := astrGrid(t, name)["SpentDayATR"]
		// Обе стороны оси дня: 0.6 (57.6% баров) и 1.5 (7.2% баров, 23 сделки за 34 месяца =
		// 7 на обучающее окно при пороге 20) — край, о цене которого предупреждает _comment,
		// но который по решению владельца в сетке остаётся и утонет под порогом сам.
		for _, v := range []float64{0.6, 1.5} {
			if !containsValue(spent, v) {
				t.Errorf("%s: SpentDayATR = %v, не содержит %v — ось дня меряется целиком", name, spent, v)
			}
		}
	}

	volume := astrGrid(t, "cal_volume.json")
	// Верх оси множителя остаётся, хотя гейт платный по всей оси (1.0 -> 0.919, 2.0 -> 1.058,
	// 4.0 -> 0.830 против 1.093 у выключенного): это предупреждение, а не основание резать край.
	for _, v := range []float64{1.0, 4.0} {
		if !containsValue(volume["VolMult"], v) {
			t.Errorf("cal_volume.json: VolMult = %v, не содержит %v — ось объёма меряется целиком", volume["VolMult"], v)
		}
	}
	// Ось базы на ASTR ЗНАЧИМА, а не шумит: при VolMult 1.5 база 3 дня -> 124 сделки/1.343,
	// 5 -> 118/1.263, 10 -> 117/1.132, 14 -> 110/0.982. Разброс 0.36 PF, оба края обязательны.
	for _, v := range []float64{3, 20} {
		if !containsValue(volume["VolBaseDays"], v) {
			t.Errorf("cal_volume.json: VolBaseDays = %v, не содержит %v — ось базы на ASTR значима, разброс 0.36 PF", volume["VolBaseDays"], v)
		}
	}

	window := astrGrid(t, "cal_vol_window.json")
	// Максимум кривой окна стоит у УЗКОГО края (1 -> 1.066, 2 -> 1.073, 3 -> 1.058, 12 -> 1.049,
	// 16 -> 1.021), как у YDEX. Верх доведён до 24 всё равно: ось меряется целиком.
	for _, v := range []float64{1, 24} {
		if !containsValue(window["VolLookbackBars"], v) {
			t.Errorf("cal_vol_window.json: VolLookbackBars = %v, не содержит %v — ось окна меряется целиком", window["VolLookbackBars"], v)
		}
	}

	risk := astrGrid(t, "cal_risk.json")
	// Строка 0.3 остаётся, и на ASTR у неё есть отдельное основание помимо решения о ширине:
	// реальный круг издержек 0.124% съедает там 13.1% риска — НИЖЕ каталожной черты 17%, по
	// которой её вырезали из domrf/ и elfv/. Дешёвая бумага (шаг 0.05 ₽ = 0.0122% цены).
	if !containsValue(risk["StopDailyATR"], 0.3) {
		t.Errorf("cal_risk.json: StopDailyATR = %v, не содержит 0.3 — узкий край жив: круг 0.124%% съедает там 13.1%% риска, ниже черты 17%%", risk["StopDailyATR"])
	}
	if !containsValue(risk["StopDailyATR"], 1.5) {
		t.Errorf("cal_risk.json: StopDailyATR = %v, не содержит 1.5 — широкий край оси риска остаётся в сетке", risk["StopDailyATR"])
	}
	for _, v := range risk["StopDailyATR"] {
		if v <= 0 {
			t.Errorf("cal_risk.json свипует StopDailyATR=%v: сделка без стопа запрещена ядром", v)
		}
	}
	// Цель обязана накрывать самый широкий стоп, иначе контрольная точка каталога не выполняется.
	if !containsValue(risk["TPDailyATR"], 2.5) {
		t.Errorf("cal_risk.json: TPDailyATR = %v, не содержит 2.5 — цель меряется целиком и обязана быть шире самого широкого стопа", risk["TPDailyATR"])
	}

	exit := astrGrid(t, "cal_exit.json")
	for _, v := range []float64{55, 85} {
		if !containsValue(exit["RSIUpper"], v) {
			t.Errorf("cal_exit.json: RSIUpper = %v, не содержит %v — полоса выхода меряется целиком", exit["RSIUpper"], v)
		}
	}

	trail := astrGrid(t, "cal_trail.json")
	if !containsValue(trail["TrailDailyATR"], 1.5) {
		t.Errorf("cal_trail.json: TrailDailyATR = %v, не содержит 1.5 — ось трейла меряется целиком", trail["TrailDailyATR"])
	}
	if got := trail["UseRSIExit"]; !sameSet(got, 0, 1) {
		t.Errorf("cal_trail.json: UseRSIExit = %v, want {0,1} — тема меряет, способен ли трейл заменить RSI-выход", got)
	}
}
```

- [ ] **Step 2: Запустить тест и убедиться, что он падает**

Run: `go test ./internal/service/backtest/ -run TestASTRGridsStayWide -v`
Expected: FAIL — каталога `data/params/rsi_pullback/astr/` не существует.

- [ ] **Step 3: Создать `cal_screen.json`**

```json
{
  "_comment": "data/params/rsi_pullback/astr/cal_screen.json — тема screen для ASTR, 4 прогона (UseDayATRGate 2 x UseVolume 2). Тема меряет цену каждого из двух опциональных гейтов в сделках и в PF поверх дефолтов ядра. Единственная тема каталога, которая запускается с -min-trades 1: при штатных 20 отфильтруются ровно те строки, которые она меряет. ЗАМЕРЫ ПОЛНОЙ ИСТОРИИ (34 месяца, без walk-forward): день включён + объём выключен (дефолт) — 158 сделок/PF 1.093; день ВЫКЛЮЧЕН — 440 сделок/0.944, то есть дневной гейт вырезает 64% сигналов и разворачивает знак edge; обе гейта выключены (день off, объём on) — 258/0.930. Дневной гейт на ASTR несёт весь edge. Команда: go run ./cmd/backtest -ticker ASTR -strategy rsi_pullback -interval Minutes30 -calibrate data/params/rsi_pullback/astr/cal_screen.json -out ./reports/ASTR_screen -months 34 -train-months 10 -test-months 6 -min-trades 1 -metric profit_factor. РЕЗУЛЬТАТ ПРОГОНА 2026-08-30: ",
  "phases": [
    {
      "name": "screen",
      "grid": {
        "UseDayATRGate": [0, 1],
        "UseVolume": [0, 1]
      }
    }
  ]
}
```

- [ ] **Step 4: Создать `cal_entry.json`**

```json
{
  "_comment": "data/params/rsi_pullback/astr/cal_entry.json — ПЕРВАЯ КЛЮЧЕВАЯ тема entry для ASTR, 378 прогонов (RSIUpper 7 x RSIPeriod 6 x RSILower 9). Тема меряет полосу RSI целиком поверх дефолтов ядра, на ней стоит планка. РЕШЕНИЕ ВЛАДЕЛЬЦА 2026-08-30: сетки держать МАКСИМАЛЬНО ШИРОКИМИ; обрезок нет, замеры ниже — предупреждения о краях, а не основание резать. Цена решения названа прямо: широкая ось повышает риск переоптимизации на обучающем окне, поэтому пофолдовая устойчивость читается строже, и край, победивший в одном фолде и мёртвый в остальных, в точку не идёт. ОСЬ ПЕРИОДА {3..8} ЦЕЛИКОМ, замеры на уровне 30: RSI(3) — 223 сделки/PF 1.274, RSI(4) — 158/1.093, RSI(5) — 113/1.080, RSI(6) — 88/1.289, RSI(7) — 74/1.498, RSI(8) — 57/1.267. ОСЬ УРОВНЯ {10..50}: рельеф МОНОТОННЫЙ И САМЫЙ СИЛЬНЫЙ В КАТАЛОГЕ — чем глубже уровень, тем выше PF. RSI(3): 10 -> 67 сделок/1.812, 15 -> 107/1.611, 20 -> 153/1.322, 25 -> 195/1.114, 30 -> 223/1.274, 35 -> 264/1.126, 40 -> 294/1.037, 45 -> 331/1.039, 50 -> 364/1.085. RSI(4): 10 -> 21/2.252, 15 -> 57/1.908, 20 -> 89/1.714, 25 -> 125/1.162, 30 -> 158/1.093, 35 -> 180/1.153, 40 -> 208/1.054, 45 -> 240/0.987, 50 -> 270/1.029. RSI(6): 10 -> 1/133.4 (одна сделка, число бессмысленно), 15 -> 15/1.176, 20 -> 33/1.898, 25 -> 63/1.565, 30 -> 88/1.289, 35 -> 110/0.999, 40 -> 143/1.116, 45 -> 159/0.918, 50 -> 201/1.017. ПРЕДУПРЕЖДЕНИЕ о глубоком крае: RSI(4)@10 даёт 21 сделку за 34 месяца = 6 на десятимесячное обучающее окно, RSI(6)@10 — ОДНУ; оба края живы формально, а не по существу, и утонут под -min-trades 20 сами. ПРЕДУПРЕЖДЕНИЕ о мелком крае: выше 50 отката нет по определению, а растяжка до 50 дважды приносила вред другим тикерам каталога (WUSH 2.000 -> 1.674). ГЛАВНЫЙ РИСК ТЕМЫ: дефолтный уровень 30 стоит далеко от рабочей зоны бумаги, весь рельеф измерен на полной истории, которую видел человек. ОСЬ ВЫХОДА {55..85}: 55 -> 182 сделки/1.006, 60 -> 175/1.242, 65 -> 165/1.098, 70 -> 158/1.093, 75 -> 155/1.086, 80 -> 153/1.177, 85 -> 149/1.088 — кривая двугорбая, внятного максимума нет. Команда: go run ./cmd/backtest -ticker ASTR -strategy rsi_pullback -interval Minutes30 -calibrate data/params/rsi_pullback/astr/cal_entry.json -out ./reports/ASTR_entry -months 34 -train-months 10 -test-months 6 -min-trades 20 -metric profit_factor. РЕЗУЛЬТАТ ПРОГОНА 2026-08-30: ",
  "phases": [
    {
      "name": "entry",
      "grid": {
        "RSIUpper": [55, 60, 65, 70, 75, 80, 85],
        "RSIPeriod": [3, 4, 5, 6, 7, 8],
        "RSILower": [10, 15, 20, 25, 30, 35, 40, 45, 50]
      }
    }
  ]
}
```

- [ ] **Step 5: Создать `cal_trend.json`**

```json
{
  "_comment": "data/params/rsi_pullback/astr/cal_trend.json — ВТОРАЯ КЛЮЧЕВАЯ тема trend для ASTR, 27 прогонов (EMAFast 3 x EMASlow 9). Тема меряет пару EMA трендового фильтра поверх дефолтов ядра, на ней стоит второй критерий планки. ДОПУСК РОВНЫЙ: доля будних баров окна с EMAFast > EMASlow держится в полосе 42.4-44.3% по ВСЕЙ таблице 5..40 x 30..200 — дефицита выборки из-за выбора пары быть не может, ось безопасна с любого края. РЕЛЬЕФА ПОЧТИ НЕТ: 5/30 -> 123 сделки/PF 1.182, 10/30 -> 141/0.952, 10/40 -> 144/1.079, 10/50 -> 149/1.100, 10/70 -> 157/1.096, 10/100 (дефолт) -> 158/1.093, 10/150 -> 161/1.023, 10/200 -> 161/1.091, 20/50 -> 160/1.135, 20/100 -> 163/0.938, 20/150 -> 160/1.027, 30/100 -> 160/1.025, 40/200 -> 162/1.069. Размах 0.94-1.18 без монотонности: ось шумит, края не выделяются. ОЖИДАНИЕ ПО ТЕМЕ, записанное ДО прогона: разброс победителей по фолдам и невзятый критерий устойчивости. Пары EMAFast >= EMASlow эта ось не порождает. Команда: go run ./cmd/backtest -ticker ASTR -strategy rsi_pullback -interval Minutes30 -calibrate data/params/rsi_pullback/astr/cal_trend.json -out ./reports/ASTR_trend -months 34 -train-months 10 -test-months 6 -min-trades 20 -metric profit_factor. РЕЗУЛЬТАТ ПРОГОНА 2026-08-30: ",
  "phases": [
    {
      "name": "trend",
      "grid": {
        "EMAFast": [5, 10, 20],
        "EMASlow": [30, 40, 50, 70, 100, 120, 150, 170, 200]
      }
    }
  ]
}
```

- [ ] **Step 6: Создать `cal_day.json` и `cal_day_spent.json`**

`cal_day.json`:

```json
{
  "_comment": "data/params/rsi_pullback/astr/cal_day.json — тема day для ASTR, 63 прогона (FreshDayATR 7 x SpentDayATR 9 при UseDayATRGate 1). Тема меряет ОБЕ ветки двустороннего дневного гейта вместе. ГЕЙТ НЕСЁТ ВЕСЬ EDGE: без него 440 сделок при PF 0.944 против 158 при 1.093 — вырезает 64% сигналов и разворачивает знак. ДОЛИ БУДНИХ БАРОВ: свежий день <=0.2 — 5.7%, <=0.3 — 11.4%, <=0.4 — 20.8%, <=0.5 — 31.6%; день исчерпан >=0.6 — 57.6%, >=0.8 — 36.7%, >=1.0 — 22.8%, >=1.25 — 12.8%, >=1.5 — 7.2%. Медиана размаха дня к текущему бару 0.67 ATR, ко второму бару дня 0.28 ATR. СВЕЖАЯ ВЕТКА РАЗБАВЛЯЕТ: 0.2 -> 185 сделок/1.053, 0.3 -> 217/1.002, 0.4 -> 263/1.027, 0.5 -> 320/0.945 против 158/1.093 при нуле. Ноль в оси обязателен — на всех прод-тикерах каталога победил он. ПРЕДУПРЕЖДЕНИЕ о крае исчерпанной ветки: SpentDayATR 1.5 оставляет 23 сделки за 34 месяца = 7 на десятимесячное обучающее окно при пороге 20, край утонет под порогом сам. Команда: go run ./cmd/backtest -ticker ASTR -strategy rsi_pullback -interval Minutes30 -calibrate data/params/rsi_pullback/astr/cal_day.json -out ./reports/ASTR_day -months 34 -train-months 10 -test-months 6 -min-trades 20 -metric profit_factor. РЕЗУЛЬТАТ ПРОГОНА 2026-08-30: ",
  "phases": [
    {
      "name": "day",
      "grid": {
        "UseDayATRGate": [1],
        "FreshDayATR": [0, 0.2, 0.25, 0.3, 0.35, 0.4, 0.5],
        "SpentDayATR": [0.6, 0.7, 0.8, 0.9, 1.0, 1.1, 1.25, 1.3, 1.5]
      }
    }
  ]
}
```

`cal_day_spent.json`:

```json
{
  "_comment": "data/params/rsi_pullback/astr/cal_day_spent.json — тема day_spent для ASTR, 10 прогонов (SpentDayATR 10 при UseDayATRGate 1 и FreshDayATR 0). Тема меряет ТОЛЬКО исчерпанную ветку дневного гейта, изолированно от свежей: cal_day.json меряет их вместе, и без этого файла нельзя отличить вклад одной ветки от вклада пары. ТОЧЕЧНЫЙ РЯД (полная история, свежая ветка выключена): 0.6 -> 252 сделки/PF 1.031, 0.7 -> 199/1.185, 0.8 -> 158/1.093, 0.9 -> 122/1.197, 1.0 -> 93/1.161, 1.1 -> 72/1.140, 1.25 -> 50/1.422, 1.3 -> 44/1.355, 1.5 -> 23/1.460. КРИВАЯ НЕМОНОТОННА, максимум на хвосте при тающей выборке — признак ШУМА, а не рельефа; это записано ДО прогона, чтобы победитель хвоста не был принят за находку. ПРЕДУПРЕЖДЕНИЕ: 1.4 в оси нет в точечном ряду, значение добавлено, чтобы хвост мерился с шагом, а не прыжком 1.3 -> 1.5. Команда: go run ./cmd/backtest -ticker ASTR -strategy rsi_pullback -interval Minutes30 -calibrate data/params/rsi_pullback/astr/cal_day_spent.json -out ./reports/ASTR_day_spent -months 34 -train-months 10 -test-months 6 -min-trades 20 -metric profit_factor. РЕЗУЛЬТАТ ПРОГОНА 2026-08-30: ",
  "phases": [
    {
      "name": "day_spent",
      "grid": {
        "UseDayATRGate": [1],
        "FreshDayATR": [0],
        "SpentDayATR": [0.6, 0.7, 0.8, 0.9, 1.0, 1.1, 1.25, 1.3, 1.4, 1.5]
      }
    }
  ]
}
```

- [ ] **Step 7: Создать `cal_volume.json` и `cal_vol_window.json`**

`cal_volume.json`:

```json
{
  "_comment": "data/params/rsi_pullback/astr/cal_volume.json — тема volume для ASTR, 35 прогонов (VolMult 7 x VolBaseDays 5 при UseVolume 1). Тема меряет слот-базовый объёмный гейт. ГЕЙТ ПЛАТНЫЙ ПО ВСЕЙ ОСИ МНОЖИТЕЛЯ (VolBaseDays 14, VolLookbackBars 3): выключен -> 158 сделок/PF 1.093, 1.0 -> 129/0.919, 1.2 -> 123/0.941, 1.5 -> 110/0.982, 2.0 -> 99/1.058, 2.5 -> 88/0.963, 3.0 -> 77/0.913, 4.0 -> 62/0.830. НО ОСЬ БАЗЫ НА ASTR ЗНАЧИМА, и это редкость в каталоге: при VolMult 1.5 база 3 дня -> 124 сделки/1.343, 5 -> 118/1.263, 10 -> 117/1.132, 14 -> 110/0.982. Разброс 0.36 PF по базе — это уже рельеф, а не шум (на BANEP разброс базы был 0.17 и признан шумом). Поэтому оба края базы обязательны: 3 (максимум замера) и 20 (проверка, продолжается ли падение). ВНИМАНИЕ НА ДЛИТЕЛЬНОСТЬ: с включённым гейтом окно прогрева растёт до 403 баров при VolBaseDays 5 и до 739 при 10 — эта тема идёт примерно вдвое медленнее прочих. Команда: go run ./cmd/backtest -ticker ASTR -strategy rsi_pullback -interval Minutes30 -calibrate data/params/rsi_pullback/astr/cal_volume.json -out ./reports/ASTR_volume -months 34 -train-months 10 -test-months 6 -min-trades 20 -metric profit_factor. РЕЗУЛЬТАТ ПРОГОНА 2026-08-30: ",
  "phases": [
    {
      "name": "volume",
      "grid": {
        "UseVolume": [1],
        "VolMult": [1.0, 1.2, 1.5, 2.0, 2.5, 3.0, 4.0],
        "VolBaseDays": [3, 5, 10, 14, 20]
      }
    }
  ]
}
```

`cal_vol_window.json`:

```json
{
  "_comment": "data/params/rsi_pullback/astr/cal_vol_window.json — тема vol_window для ASTR, 40 прогонов (VolLookbackBars 8 x VolMult 5 при UseVolume 1). Тема меряет окно объёмного гейта — сколько баров назад берётся сравниваемый объём. ТОЧЕЧНЫЙ РЯД (VolMult 2.0, база 14): 1 -> 85 сделок/PF 1.066, 2 -> 91/1.073, 3 -> 99/1.058, 5 -> 111/0.927, 8 -> 117/0.945, 12 -> 131/1.049, 16 -> 140/1.021. Максимум у УЗКОГО края (2), как у YDEX. МЕСТО В РЯДУ КАТАЛОГА (седьмой замер этой оси): ELFV (18.9% дней короче 20 баров) — максимум на дефолте 3, BANEP (18.4%) — на 16, DIAS (11.9%) — на 12, BSPB (7.7%) — на 5, YDEX (0.2%) — на 1, ASTR (1.5%) — на 2. Правило «чем ликвиднее бумага и чем меньше коротких дней, тем ближе оптимум к дефолту» на ASTR подтверждается. ОЖИДАНИЕ, записанное ДО прогона: тема малозначима, коротких дней всего 1.5% (11 из 736), слот-базовая линия почти всегда опирается на полный день; отрицательный результат темы — часть протокола, а не повод её пропустить. Верх оси доведён до 24, чтобы кривая была видна целиком. Команда: go run ./cmd/backtest -ticker ASTR -strategy rsi_pullback -interval Minutes30 -calibrate data/params/rsi_pullback/astr/cal_vol_window.json -out ./reports/ASTR_vol_window -months 34 -train-months 10 -test-months 6 -min-trades 20 -metric profit_factor. РЕЗУЛЬТАТ ПРОГОНА 2026-08-30: ",
  "phases": [
    {
      "name": "vol_window",
      "grid": {
        "UseVolume": [1],
        "VolLookbackBars": [1, 2, 3, 5, 8, 12, 16, 24],
        "VolMult": [1.0, 1.2, 1.5, 2.0, 2.5]
      }
    }
  ]
}
```

- [ ] **Step 8: Создать `cal_risk.json`**

```json
{
  "_comment": "data/params/rsi_pullback/astr/cal_risk.json — тема risk для ASTR, 48 прогонов (StopDailyATR 6 x TPDailyATR 8). Тема меряет стоп и цель в долях ДНЕВНОГО ATR. ДНЕВНОЙ ATR(14): медиана 3.16% цены, p10 2.41, p90 4.70 (n = 706) — выше BANEP (2.86%) и YDEX (2.84%). ВЫЖИВАЕМОСТЬ И ИЗДЕРЖКИ (реальный круг 0.124%, шаг цены 0.05 ₽ = 0.0122% при медианной цене 408 ₽): стоп 0.3 = 0.95% цены, круг съедает 13.1% риска, размах дня достаёт уровня в 99.3% дней; 0.5 = 1.58%/7.8%/88.9%; 0.7 = 2.21%/5.6%/67.9%; 0.8 = 2.53%/4.9%/55.2%; 1.0 = 3.16%/3.9%/37.3%; 1.25 = 3.95%/3.1%/22.4%; 1.3 = 4.11%/3.0%/20.1%; 1.5 = 4.74%/2.6%/13.5%. Строка 0.3 ОСТАВЛЕНА и на ASTR имеет собственное основание помимо решения о ширине: 13.1% НИЖЕ каталожной черты 17%, по которой её вырезали из domrf/ и elfv/ — бумага дешёвая. КАПКАН ШИРОКОГО СТОПА В ЧИСТОМ ВИДЕ (цель 0.6): 0.3 -> 165 сделок/PF 0.838, 0.5 -> 158/1.093, 0.7 -> 148/1.419, 0.8 -> 147/1.296, 1.0 -> 147/1.881, 1.25 -> 146/2.150, 1.3 -> 146/2.134, 1.5 -> 146/2.168. ЧИСЛО СДЕЛОК СТОИТ НА МЕСТЕ начиная с 0.7, а PF растёт вдвое: убыток вытесняется в RSI-выход, где не виден как срабатывание стопа. В отличие от BANEP, издержками этот рост объяснить почти нечем (круг 0.124%), значит доля капкана здесь ВЫШЕ. ТРЕБОВАНИЕ К ПОБЕДИТЕЛЮ ТЕМЫ: обязательно проверить долю выходов по SL. ОСЬ ЦЕЛИ (стоп 0.5): 0.4 -> 165/1.068, 0.5 -> 159/1.052, 0.6 -> 158/1.093, 0.8 -> 155/1.117, 1.0 -> 155/1.146, 1.5 -> 155/1.036, 2.0 -> 155/1.025, 2.5 -> 155/1.025. Колонки 2.0 и 2.5 совпадают ПОБАЙТОВО — цель шире ~1.5 дневного ATR на ASTR не достигается; края оставлены, чтобы это было видно в отчёте, и чтобы выполнялась контрольная точка TestRSIPullbackGridControlPoints (максимум цели строго больше максимума стопа). ПАРЫ, ЗАМЕРЕННЫЕ ОТДЕЛЬНО: 0.7/1.0 -> 1.553, 1.0/1.0 -> 1.927, 1.3/1.5 -> 2.100. Команда: go run ./cmd/backtest -ticker ASTR -strategy rsi_pullback -interval Minutes30 -calibrate data/params/rsi_pullback/astr/cal_risk.json -out ./reports/ASTR_risk -months 34 -train-months 10 -test-months 6 -min-trades 20 -metric profit_factor. РЕЗУЛЬТАТ ПРОГОНА 2026-08-30: ",
  "phases": [
    {
      "name": "risk",
      "grid": {
        "StopDailyATR": [0.3, 0.5, 0.7, 1.0, 1.3, 1.5],
        "TPDailyATR": [0.4, 0.5, 0.6, 0.8, 1.0, 1.5, 2.0, 2.5]
      }
    }
  ]
}
```

- [ ] **Step 9: Создать `cal_exit.json` и `cal_trail.json`**

`cal_exit.json`:

```json
{
  "_comment": "data/params/rsi_pullback/astr/cal_exit.json — тема exit для ASTR, 7 прогонов (RSIUpper 7). Тема меряет уровень RSI-выхода отдельной осью: cal_entry.json свипует его тоже, но там он третья ось при 378 комбинациях и его собственный рельеф не читается. ТОЧЕЧНЫЙ РЯД: 55 -> 182 сделки/PF 1.006, 60 -> 175/1.242, 65 -> 165/1.098, 70 (дефолт) -> 158/1.093, 75 -> 155/1.086, 80 -> 153/1.177, 85 -> 149/1.088; ВЫКЛЮЧЕН -> 147/1.003. Кривая ДВУГОРБАЯ без внятного максимума — ось шумит; при этом сам выход несёт работу: без него PF падает до 1.003. Ось меряется целиком с обоих краёв, мёртвой зоны на ASTR нет (в отличие от BANEP, где всё выше 75 давало ниже единицы). Команда: go run ./cmd/backtest -ticker ASTR -strategy rsi_pullback -interval Minutes30 -calibrate data/params/rsi_pullback/astr/cal_exit.json -out ./reports/ASTR_exit -months 34 -train-months 10 -test-months 6 -min-trades 20 -metric profit_factor. РЕЗУЛЬТАТ ПРОГОНА 2026-08-30: ",
  "phases": [
    {
      "name": "exit",
      "grid": {
        "RSIUpper": [55, 60, 65, 70, 75, 80, 85]
      }
    }
  ]
}
```

`cal_trail.json`:

```json
{
  "_comment": "data/params/rsi_pullback/astr/cal_trail.json — тема trail для ASTR, 14 прогонов (UseRSIExit 2 x TrailDailyATR 7 при UseTrail 1). Тема меряет, способен ли трейлинг-стоп заменить RSI-выход или дополнить его. ТОЧЕЧНЫЙ РЯД: 0.3 -> 178 сделок/PF 0.919, 0.5 -> 161/0.958, 0.7 -> 159/1.053, 1.0 -> 158/1.093, 1.3 -> 158/1.093. Значения 1.0 и 1.3 совпадают с baseline ПОБАЙТОВО — трейл шире 0.7 дневного ATR на ASTR не срабатывает НИ РАЗУ. ОЖИДАНИЕ ОТРИЦАТЕЛЬНОЕ, записано ДО прогона: узкий трейл режет PF, широкий не работает. Тема прогоняется целиком, потому что её отрицательный результат — часть протокола и основание не включать трейл в точку, а не потому, что от неё ждут находки. Ноль в оси оставлен: это конфигурация «трейл включён флагом, но дистанция нулевая», и её поведение обязано быть видно в лидерборде рядом с рабочими дистанциями. Команда: go run ./cmd/backtest -ticker ASTR -strategy rsi_pullback -interval Minutes30 -calibrate data/params/rsi_pullback/astr/cal_trail.json -out ./reports/ASTR_trail -months 34 -train-months 10 -test-months 6 -min-trades 20 -metric profit_factor. РЕЗУЛЬТАТ ПРОГОНА 2026-08-30: ",
  "phases": [
    {
      "name": "trail",
      "grid": {
        "UseRSIExit": [0, 1],
        "UseTrail": [1],
        "TrailDailyATR": [0, 0.3, 0.5, 0.7, 1.0, 1.3, 1.5]
      }
    }
  ]
}
```

- [ ] **Step 10: Запустить тесты**

Run: `go test ./internal/service/backtest/ -run 'TestASTRGridsStayWide|TestRSIPullbackCalFilesValid|TestRSIPullbackGridControlPoints' -v`
Expected: PASS все три. `TestRSIPullbackCalFilesValid` проверяет, что в `_comment` каждого файла
есть собственный путь и команда запуска; `TestRSIPullbackGridControlPoints` — что максимум
`TPDailyATR` строго больше максимума `StopDailyATR`.

- [ ] **Step 11: Коммит**

```bash
git add data/params/rsi_pullback/astr internal/service/backtest/rsi_pullback_astr_grid_test.go
git commit -m "feat(rsi_pullback): каталог широких сеток ASTR и сторожевой тест осей"
```

---

### Task 2: Пакет `strategy/astr` в состоянии «калибровка не проводилась»

**Files:**
- Create: `internal/service/trading_strategy/rsi_pullback/strategy/astr/astr.go`
- Create: `internal/service/trading_strategy/rsi_pullback/strategy/astr/astr_test.go`
- Modify: `internal/service/backtest/rsi_pullback_registry.go`
- Modify: `internal/service/backtest/rsi_pullback_registry_test.go`

**Interfaces:**
- Consumes: `core.Params`, `core.DefaultParams()` из
  `internal/service/trading_strategy/rsi_pullback/strategy/core`.
- Produces: `astr.Ticker` (строка `"ASTR"`) и `astr.DefaultParams() core.Params` — их используют
  Task 12 (литерал) и Task 13 (реестр живого раннера).

- [ ] **Step 1: Написать падающий тест baseline-состояния**

Создать `internal/service/trading_strategy/rsi_pullback/strategy/astr/astr_test.go`:

```go
package astr

import (
	"testing"

	"tinvest/internal/service/trading_strategy/rsi_pullback/strategy/core"
)

// TestParamsTrackTheBaselineUntilCalibrated фиксирует ЧЕСТНОЕ состояние: калибровка ASTR ещё не
// проводилась, поэтому пакет обязан возвращать ровно baseline ядра. Тест держит это состояние до
// Task 12, где его заменяет снимок литерала. Пока он стоит, ни одна правка не может тихо
// подсунуть в прод «почти откалиброванные» параметры.
func TestParamsTrackTheBaselineUntilCalibrated(t *testing.T) {
	if got, want := DefaultParams(), core.DefaultParams(); got != want {
		t.Fatalf("ASTR ещё не откалиброван, параметры обязаны совпадать с baseline:\n got: %+v\nwant: %+v", got, want)
	}
}

func TestTickerIsASTR(t *testing.T) {
	if Ticker != "ASTR" {
		t.Fatalf("Ticker = %q, want ASTR", Ticker)
	}
}
```

- [ ] **Step 2: Запустить тест и убедиться, что он падает**

Run: `go test ./internal/service/trading_strategy/rsi_pullback/strategy/astr/ -v`
Expected: FAIL — пакета `astr` не существует.

- [ ] **Step 3: Создать пакет**

Создать `internal/service/trading_strategy/rsi_pullback/strategy/astr/astr.go`:

```go
// Package astr supplies the ticker and rsi_pullback Params for ASTR (Группа Астра, обыкновенные
// акции, лот 1).
//
// СОСТОЯНИЕ: КАЛИБРОВКА НЕ ПРОВОДИЛАСЬ. Пакет возвращает core.DefaultParams() — baseline ядра, не
// подобранный под этот инструмент. Так и должно быть до конца калибровки: пакет заведён заранее,
// чтобы прогоны шли через тот же реестр, что и у остальных восемнадцати тикеров, а не через
// generic-ветку. Состояние держит astr_test.go.
//
// ОКНО КОРОЧЕ КАТАЛОЖНОГО. Первый бар получасовой серии — 2023-10-13 (IPO Группы Астра), доступно
// 34.5 месяца, каноническая схема 36/12/6 не помещается. Расчётное окно — 2023-10-29 … 2026-08-29
// (34 месяца; в окне 34 353 получасовых бара, из них 24 287 будних; дневная серия 848 свечей, в
// окне 719 будних).
//
// СХЕМА ПРОГОНОВ АДАПТИРОВАННАЯ: -months 34 -train-months 10 -test-months 6 -min-trades 20
// -metric profit_factor, четыре фолда встык (10 + 6x4 = 34). Схема жертвует ДЛИНОЙ обучающего
// окна, а не числом фолдов: правило сборки точки «>=3 фолда из 4» при трёх фолдах вырождается в
// «3 из 3» и перестаёт различать устойчивость. Прецеденты адаптации: IVAT 25/9/4, DIAS 30/10/5,
// YDEX 25/9/4. Числа ASTR сопоставимы с ними по духу, а с 36-месячным каталогом — лишь
// качественно. Цена схемы: обучающее окно 10 месяцев при пороге 20 сделок требует ~2 сделки в
// месяц; baseline даёт 4.6, запас двукратный, но темы, режущие выборку, потеряют часть комбинаций
// под порогом — это ожидаемое поведение сетки, а не дефект.
//
// ЗАПАС ИСТОРИИ 16 ДНЕЙ, на прогрев дневного ATR(14) хватает впритык. -refresh во время калибровки
// НЕ запускать — он сдвинет обе границы и сделает все замеры ниже несравнимыми.
//
// СЕТКИ ЭТОГО ТИКЕРА ДЕРЖАТСЯ МАКСИМАЛЬНО ШИРОКИМИ — решение владельца 2026-08-30, то же, что по
// BANEP. Обрезок осей нет; замеры, которые в узком каталоге были бы основанием вырезать край,
// живут в _comment сеток как предупреждения. Сторожевой тест TestASTRGridsStayWide запрещает
// УРЕЗАТЬ ось, а не расширять её. Жёстких инвариантов три: RSILower <= 50, RSIPeriod >= 3, ось
// тренда не порождает пар EMAFast >= EMASlow.
//
// АПРИОР, записанный ДО прогонов. Скринер (pullback_screen_Minutes30_20260804_232456.md, строка 16
// из 99 прошедших вселенную, сразу за BANEP): оборот 299 млн ₽, дневной ATR 3.47%, баров 33 659,
// TradesMed 35, PFmed 1.51, Capped 4/24, SilentCfg 0/24, плато 67%, PFmed HO 2.44 на 7 сделках.
// Контрольный прогон дефолтов ядра на расчётных 34 месяцах: 158 сделок, PF 1.093, net +7278 ₽ —
// ПРЕДПОСЛЕДНЕЕ место в ряду baseline каталога (YDEX 1.778, LSNGP 1.554, ELFV 1.546, IVAT 1.432,
// LENT 1.417, SVAV 1.368, BANEP 1.336, BSPB 1.114, ASTR 1.093, SIBN 1.027, NVTK 0.984). Веса
// априору не придаётся: вопрос «предсказывают ли колонки скринера исход протокола» каталог закрыл
// шесть раз подряд (SIBN, ELFV, DIAS, BSPB, YDEX, BANEP) — не предсказывают ни снизу, ни сверху.
//
// ПРОЦЕДУРА ТЕМ КАНОНИЧЕСКАЯ: все десять тем идут поверх дефолтов ядра, якоря нет, поздних сеток
// нет. Оговорка, которую надо держать при чтении темы entry: по УРОВНЮ входа дефолт 30 стоит
// далеко от рабочей зоны бумаги, поэтому baseline занижает потенциал ASTR сильнее, чем у соседей.
//
// ЧТО ЗНАЕМ ОБ ИНСТРУМЕНТЕ ДО ПРОГОНОВ (замеры на расчётном окне):
//
//   - Режим САМЫЙ ТЯЖЁЛЫЙ В КАТАЛОГЕ: итог окна −57.7% (497.2 -> 210.1), максимальная просадка
//     −82.9% (пик 2024-03-27 -> дно 2026-07-17). Растёт ОДНО полугодие из шести, и оно первое
//     (+17.7%; далее −8.0%, −24.2%, −7.1%, −34.7%, −15.5%). Та же форма, что у BANEP, но глубже.
//     Разбивка принятой точки по полугодиям ОБЯЗАТЕЛЬНА; точка, чей net держится одним
//     полугодием, вердикта не получает.
//   - ИЗДЕРЖКИ ДЕШЁВЫЕ — редкий плюс. Шаг цены 0.05 ₽ при медианной цене 408 ₽ = 0.0122%
//     (уровень YDEX 0.0123%, вчетверо дешевле BANEP 0.0436%), реальный круг ~0.124% против
//     моделируемых 0.1%. При этом baseline уходит в убыток уже при удвоенном круге: 1.093 ->
//     0.892 (0.2%) -> 0.721 (0.3%) — это свойство СЛАБОГО baseline, а не дорогой бумаги.
//   - ЛИКВИДНОСТЬ ХОРОШАЯ: оборот по будним дням медиана 214 млн ₽, p10 85 млн, p90 809 млн
//     (n = 736) — запас над гейтом вселенной 50 млн есть даже в десятом процентиле, в отличие от
//     BANEP (p10 ровно 49 млн). Баров в буднем дне: медиана 35, p10 29, минимум 18; дней короче
//     20 баров — 1.5% (11 из 736), уровень YDEX.
//   - ДИВИДЕНДНОГО РИСКА ПРАКТИЧЕСКИ НЕТ: гэпы открытия медиана +0.03%, p5 −0.44%, p95 +0.75%
//     (n = 718); за всё окно РОВНО ОДИН гэп хуже −3% (2024-06-13, −6.11%, обвал рынка, не
//     отсечка). Календарь отсечек живому раннеру ASTR не нужен — это отличие от BANEP, где
//     отсечка стоила −12.74% / −10.67% / −5.28% за ночь.
//   - ТРЕНДОВЫЙ ДОПУСК РОВНЫЙ: доля будних баров с EMAFast > EMASlow держится в полосе 42.4-44.3%
//     по всей таблице 5..40 x 30..200. Рельефа по паре EMA почти нет (размах 0.94-1.18 без
//     монотонности) — ожидание по теме trend: разброс победителей по фолдам.
//   - ВОЛАТИЛЬНОСТЬ ВЫСОКАЯ: дневной ATR(14) медиана 3.16% цены, p10 2.41, p90 4.70 — выше BANEP
//     (2.86%) и YDEX (2.84%).
//   - КАПКАН ШИРОКОГО СТОПА В ЧИСТОМ ВИДЕ: при цели 0.6 стоп 0.3 -> 165 сделок/PF 0.838,
//     0.5 -> 158/1.093, 0.7 -> 148/1.419, 1.0 -> 147/1.881, 1.25 -> 146/2.150, 1.5 -> 146/2.168.
//     Число сделок стоит на месте с 0.7, PF растёт вдвое — убыток вытесняется в RSI-выход.
//     Издержками это почти не объясняется (круг 0.124%), значит доля капкана выше, чем на BANEP;
//     победитель темы risk обязан проверяться по доле выходов по SL.
//   - ЦЕЛЬ шире ~1.5 дневного ATR НЕ ДОСТИГАЕТСЯ: колонки 2.0 и 2.5 совпадают побайтово.
//   - ДНЕВНОЙ ГЕЙТ НЕСЁТ ВЕСЬ EDGE: без него 440 сделок при PF 0.944 против 158 при 1.093.
//     Исчерпанная ветка при этом шумит: кривая немонотонна, максимум на хвосте при тающей
//     выборке (1.25 -> 50 сделок/1.422, 1.5 -> 23/1.460).
//   - ОБЪЁМНЫЙ ГЕЙТ ВРЕДИТ по оси множителя, НО ОСЬ БАЗЫ ЗНАЧИМА: при VolMult 1.5 база 3 дня ->
//     124 сделки/1.343, 5 -> 118/1.263, 10 -> 117/1.132, 14 -> 110/0.982. Разброс 0.36 PF.
//   - EDGE ЖИВЁТ В ГЛУБОКОЙ ПЕРЕПРОДАННОСТИ, и рельеф МОНОТОННЫЙ — самый чистый сигнал входа в
//     каталоге: RSI(4)@20 -> 89 сделок/1.714, RSI(3)@15 -> 107/1.611, RSI(6)@20 -> 33/1.898
//     против baseline 158/1.093. Это главный кандидат в точку и одновременно главный риск
//     переподгонки: дефолтный уровень 30 стоит далеко от рабочей зоны, а рельеф измерен на
//     полной истории, которую видел человек.
package astr

import "tinvest/internal/service/trading_strategy/rsi_pullback/strategy/core"

// Ticker is the instrument this package parameterises.
const Ticker = "ASTR"

// DefaultParams returns the baseline until calibration replaces it with a literal.
func DefaultParams() core.Params {
	return core.DefaultParams()
}
```

- [ ] **Step 4: Зарегистрировать тикер в реестре бэктеста**

В `internal/service/backtest/rsi_pullback_registry.go` добавить импорт в группу `rsipullback*`
(алфавитный порядок — перед `rsipullbackbanep`):

```go
	rsipullbackastr "tinvest/internal/service/trading_strategy/rsi_pullback/strategy/astr"
```

и строку в карту `rsiPullbackRegistry`:

```go
	rsipullbackastr.Ticker: rsiPullbackBindingFor(rsipullbackastr.Ticker, rsipullbackastr.DefaultParams),
```

- [ ] **Step 5: Добавить сторожевой тест реестра**

В `internal/service/backtest/rsi_pullback_registry_test.go` — импорт
`rsipullbackastr "tinvest/internal/service/trading_strategy/rsi_pullback/strategy/astr"` и тест:

```go
// TestRSIPullbackASTRTracksBaseline сторожит ЧЕСТНОЕ состояние: ASTR заведён в реестр 2026-08-30
// ДО калибровки и обязан отдавать ровно core.DefaultParams(). Тест заменяется снимком литерала в
// Task 12 плана 2026-08-30-astr-rsi-pullback-prep.md.
func TestRSIPullbackASTRTracksBaseline(t *testing.T) {
	b, ok := rsiPullbackRegistry[rsipullbackastr.Ticker]
	if !ok {
		t.Fatal("ASTR отсутствует в rsiPullbackRegistry: тикер провалится в generic-ветку")
	}
	p, pok := b.DefaultParams().(core.Params)
	if !pok {
		t.Fatalf("ASTR: DefaultParams() вернул %T, want core.Params", b.DefaultParams())
	}
	if want := core.DefaultParams(); p != want {
		t.Fatalf("ASTR ещё не откалиброван:\n got: %+v\nwant: %+v", p, want)
	}
	if got := b.Build(p).Ticker(); got != "ASTR" {
		t.Fatalf("Ticker() = %q, want ASTR", got)
	}
}
```

Форма взята из `TestRSIPullbackBANEPIsRegisteredAndCalibrated` того же файла: реестр — карта
`rsiPullbackRegistry`, `Binding.DefaultParams()` отдаёт `any`, приводится к `core.Params`.

- [ ] **Step 6: Запустить тесты**

Run: `go test ./internal/service/trading_strategy/rsi_pullback/... ./internal/service/backtest/ -v`
Expected: PASS.

- [ ] **Step 7: Проверить, что прогон идёт через реестр**

```bash
go run ./cmd/backtest -ticker ASTR -strategy rsi_pullback -interval Minutes30 \
  -out ./reports/ASTR_probe -months 34
```

Expected: **158 сделок, PF 1.093, net +7278 ₽** — те же числа, что в спеке. Расхождение означает,
что кэш изменился или прогон идёт не через реестр; в этом случае остановиться и разобраться до
первой темы, потому что все замеры спеки посчитаны на этом кэше.

- [ ] **Step 8: Коммит**

```bash
git add internal/service/trading_strategy/rsi_pullback/strategy/astr internal/service/backtest
git commit -m "feat(rsi_pullback): пакет и реестр ASTR в состоянии «калибровка не проводилась»"
```

---

### Task 3: Тема `screen` — цена двух опциональных гейтов

**Files:**
- Modify: `data/params/rsi_pullback/astr/cal_screen.json` (только `_comment`)

**Interfaces:**
- Consumes: каталог из Task 1, реестр из Task 2.
- Produces: числа цены гейтов, на которые ссылаются дока пакета и разбор Task 15.

- [ ] **Step 1: Прогнать тему**

```bash
go run ./cmd/backtest -ticker ASTR -strategy rsi_pullback -interval Minutes30 \
  -calibrate data/params/rsi_pullback/astr/cal_screen.json -out ./reports/ASTR_screen \
  -months 34 -train-months 10 -test-months 6 -min-trades 1 -metric profit_factor
```

- [ ] **Step 2: Прочитать отчёт**

Открыть свежий файл в `reports/ASTR_screen/`. Выписать для каждой из четырёх комбинаций: pooled OOS
PF, число сделок в пуле, выбор по фолдам. Ожидание из точечных замеров (полная история, без
walk-forward): день включён + объём выключен — 158 сделок/1.093; день выключен — 440/0.944; обе
выключены — 258/0.930. Прогон темы считает то же по фолдам, и числа будут другими — сравнивать надо
порядок, а не значения.

- [ ] **Step 3: Записать результат в `_comment` сетки**

Дописать после `РЕЗУЛЬТАТ ПРОГОНА 2026-08-30:` числа всех четырёх комбинаций и вывод: сколько стоит
каждый гейт в сделках и в PF. Обязательно отметить, подтвердил ли прогон точечный замер, что
дневной гейт разворачивает знак edge (0.944 без него против 1.093 с ним).

- [ ] **Step 4: Коммит**

```bash
git add data/params/rsi_pullback/astr/cal_screen.json
git commit -m "feat(rsi_pullback): ASTR, тема screen прогнана"
```

---

### Task 4: Тема `entry` — ключевая, монотонный рельеф уровня

**Files:**
- Modify: `data/params/rsi_pullback/astr/cal_entry.json` (только `_comment`)

**Interfaces:**
- Consumes: каталог из Task 1, реестр из Task 2.
- Produces: победителя pooled OOS (`RSIPeriod`, `RSILower`, `RSIUpper`) — его читает Task 11 при
  сборке точки.

- [ ] **Step 1: Прогнать тему**

```bash
go run ./cmd/backtest -ticker ASTR -strategy rsi_pullback -interval Minutes30 \
  -calibrate data/params/rsi_pullback/astr/cal_entry.json -out ./reports/ASTR_entry \
  -months 34 -train-months 10 -test-months 6 -min-trades 20 -metric profit_factor
```

- [ ] **Step 2: Выписать числа планки**

Из отчёта выписать: pooled OOS PF, число сделок в пуле, разбивку по четырём фолдам (PF и сделки
каждого), выбор ведущей оси `RSILower` в каждом фолде, выбор `RSIPeriod` и `RSIUpper` в каждом
фолде, in-sample PF каждого фолда (для оценки разрыва с OOS).

Критерий 1 планки: pooled OOS PF ≥ 1.5 при ≥ 20 сделках.
Критерий 2 планки: `RSILower` выбран одинаково в ≥ 3 фолдах из 4.
Критерий 3: фолд без единой убыточной сделки в пользу тикера не засчитывается — если такой есть,
отметить отдельной строкой.

- [ ] **Step 3: Проверить, устоял ли монотонный рельеф уровня**

Это главная проверка темы и прямая проверка риска 4 спеки. На полной истории рельеф монотонный:
чем глубже `RSILower`, тем выше PF (RSI(4): 10 → 2.252, 20 → 1.714, 30 → 1.093, 45 → 0.987).
Выписать отдельной строкой, выбрали ли фолды глубокие уровни (10–20) или разбежались. Если рельеф
пофолдово не воспроизвёлся — записать это честно: он оказался свойством полной истории, а не
бумаги, и в точку глубокий уровень не идёт.

- [ ] **Step 4: Проверить края оси периода**

Если победил `RSIPeriod = 8` (верхний край) или `3` (нижний), проверить точечным прогоном, не
растёт ли результат за краем. Пример для проверки семёрки при победившем уровне 20 (заменить
`"RSILower":20` на победивший уровень перед запуском):

```bash
cat > /tmp/astr_p7.json <<'EOF'
{"RSIPeriod":7,"RSILower":20,"RSIUpper":70,"EMAFast":10,"EMASlow":100,"DailyATRPeriod":14,"UseDayATRGate":1,"FreshDayATR":0,"SpentDayATR":0.8,"StopDailyATR":0.5,"TPDailyATR":0.6,"UseVolume":0,"VolBaseDays":14,"VolLookbackBars":3,"VolMult":1.2,"UseRSIExit":1,"UseTrail":0,"TrailDailyATR":0}
EOF
go run ./cmd/backtest -ticker ASTR -strategy rsi_pullback -interval Minutes30 \
  -params /tmp/astr_p7.json -out ./reports/ASTR_entry_edge -months 34
```

Ожидание из замеров спеки: RSI(7) на уровне 30 даёт 74 сделки при PF 1.498 — верх оси на ASTR жив,
в отличие от BANEP. Записать факт в `_comment`.

- [ ] **Step 5: Записать результат в `_comment` сетки**

Дописать после `РЕЗУЛЬТАТ ПРОГОНА 2026-08-30:` все числа Steps 2–4 и **вердикт по обоим критериям
планки отдельными фразами** («критерий PF взят/не взят: …», «критерий устойчивости взят/не взят:
…»). Отдельной фразой — разрыв in-sample и OOS: на широкой оси он у BANEP вырос двукратно, и на
ASTR его надо замерить, а не предполагать.

- [ ] **Step 6: Коммит**

```bash
git add data/params/rsi_pullback/astr/cal_entry.json
git commit -m "feat(rsi_pullback): ASTR, тема entry прогнана"
```

---

### Task 5: Тема `trend` — вторая ключевая

**Files:**
- Modify: `data/params/rsi_pullback/astr/cal_trend.json` (только `_comment`)

**Interfaces:**
- Consumes: каталог из Task 1, реестр из Task 2.
- Produces: победителя pooled OOS (`EMAFast`, `EMASlow`) — его читает Task 11.

- [ ] **Step 1: Прогнать тему**

```bash
go run ./cmd/backtest -ticker ASTR -strategy rsi_pullback -interval Minutes30 \
  -calibrate data/params/rsi_pullback/astr/cal_trend.json -out ./reports/ASTR_trend \
  -months 34 -train-months 10 -test-months 6 -min-trades 20 -metric profit_factor
```

- [ ] **Step 2: Выписать числа планки**

Из отчёта: pooled OOS PF, сделки пула, разбивка по четырём фолдам, выбор `EMASlow` в каждом фолде
(это ведущая ось темы), выбор `EMAFast` в каждом фолде.

Критерий 1: pooled OOS PF ≥ 1.5 при ≥ 20 сделках.
Критерий 2: `EMASlow` выбран одинаково в ≥ 3 фолдах из 4.

- [ ] **Step 3: Сверить с ожиданием, записанным до прогона**

В `_comment` сетки записано ожидание: ось шумит (размах 0.94–1.18 без монотонности), значит
победители по фолдам разбегутся и критерий устойчивости не будет взят. Записать, подтвердилось ли
ожидание. Если `EMASlow` неожиданно сошёлся в 3–4 фолдах — это находка, и её надо отметить
отдельно, потому что она противоречит точечным замерам.

- [ ] **Step 4: Записать результат в `_comment` сетки**

Дописать после `РЕЗУЛЬТАТ ПРОГОНА 2026-08-30:` числа Steps 2–3 и вердикт по обоим критериям планки
отдельными фразами.

- [ ] **Step 5: Коммит**

```bash
git add data/params/rsi_pullback/astr/cal_trend.json
git commit -m "feat(rsi_pullback): ASTR, тема trend прогнана"
```

---

### Task 6: Темы `day` и `day_spent` — дневной гейт

**Files:**
- Modify: `data/params/rsi_pullback/astr/cal_day.json` (только `_comment`)
- Modify: `data/params/rsi_pullback/astr/cal_day_spent.json` (только `_comment`)

**Interfaces:**
- Consumes: каталог из Task 1, реестр из Task 2.
- Produces: победителей `FreshDayATR` и `SpentDayATR` — их читает Task 11.

- [ ] **Step 1: Прогнать обе темы**

```bash
go run ./cmd/backtest -ticker ASTR -strategy rsi_pullback -interval Minutes30 \
  -calibrate data/params/rsi_pullback/astr/cal_day.json -out ./reports/ASTR_day \
  -months 34 -train-months 10 -test-months 6 -min-trades 20 -metric profit_factor

go run ./cmd/backtest -ticker ASTR -strategy rsi_pullback -interval Minutes30 \
  -calibrate data/params/rsi_pullback/astr/cal_day_spent.json -out ./reports/ASTR_day_spent \
  -months 34 -train-months 10 -test-months 6 -min-trades 20 -metric profit_factor
```

- [ ] **Step 2: Выписать числа обеих тем**

Для каждой: pooled OOS PF, сделки пула, разбивка по фолдам, выбор `SpentDayATR` в каждом фолде;
для `day` дополнительно — выбор `FreshDayATR` в каждом фолде.

- [ ] **Step 3: Проверить, победил ли ноль в свежей ветке**

На всех прод-тикерах каталога побеждал `FreshDayATR = 0`. Выписать, что выбрали фолды ASTR. Если
ненулевая свежая ветка победила — сверить с точечным замером, где она разбавляет (0.2 → 185
сделок/1.053 против 158/1.093 при нуле), и записать расхождение отдельной строкой.

- [ ] **Step 4: Проверить, не победил ли шумовой хвост исчерпанной ветки**

Точечный ряд `day_spent` немонотонен, максимум на хвосте при тающей выборке (1.25 → 50 сделок,
1.5 → 23). Если фолды выбрали хвост (≥ 1.25), выписать число сделок каждого фолда: значение,
опирающееся на выборку ниже `-min-trades 20` в большинстве фолдов, в точку не идёт. Это прямое
применение правила «три фолда из четырёх».

- [ ] **Step 5: Записать результаты в `_comment` обеих сеток**

В каждый файл — после `РЕЗУЛЬТАТ ПРОГОНА 2026-08-30:` свои числа. В `cal_day.json` дополнительно
записать, что дала пара веток вместе против одной исчерпанной (сравнение с `cal_day_spent.json`) —
ради этого сравнения второй файл и существует.

- [ ] **Step 6: Коммит**

```bash
git add data/params/rsi_pullback/astr/cal_day.json data/params/rsi_pullback/astr/cal_day_spent.json
git commit -m "feat(rsi_pullback): ASTR, темы day и day_spent прогнаны"
```

---

### Task 7: Тема `volume` — объёмный гейт и значимая ось базы

**Files:**
- Modify: `data/params/rsi_pullback/astr/cal_volume.json` (только `_comment`)

**Interfaces:**
- Consumes: каталог из Task 1, реестр из Task 2.
- Produces: победителей `UseVolume`, `VolMult`, `VolBaseDays` — их читает Task 11.

- [ ] **Step 1: Прогнать тему**

```bash
go run ./cmd/backtest -ticker ASTR -strategy rsi_pullback -interval Minutes30 \
  -calibrate data/params/rsi_pullback/astr/cal_volume.json -out ./reports/ASTR_volume \
  -months 34 -train-months 10 -test-months 6 -min-trades 20 -metric profit_factor
```

Тема идёт примерно вдвое дольше прочих: с включённым гейтом окно прогрева растёт до 403 баров при
`VolBaseDays 5` и до 739 при 10.

- [ ] **Step 2: Выписать числа**

pooled OOS PF, сделки пула, разбивка по фолдам, выбор `VolMult` и `VolBaseDays` в каждом фолде.

- [ ] **Step 3: Сравнить с выключенным гейтом**

Взять из Task 3 (тема `screen`) pooled OOS комбинации «день включён + объём выключен» и сравнить с
лучшим результатом этой темы. Если включённый гейт не превосходит выключенный — в точку идёт
`UseVolume = 0`, как на большинстве тикеров каталога. Точечный замер полной истории говорит именно
это: гейт платный по всей оси множителя (лучшее 1.058 при 2.0 против 1.093 у выключенного).

- [ ] **Step 4: Проверить, воспроизвелась ли значимость оси базы**

На полной истории ось базы даёт разброс 0.36 PF (при `VolMult 1.5`: 3 дня → 1.343, 14 → 0.982) —
это редкость в каталоге, на BANEP разброс базы был 0.17 и признан шумом. Выписать, сошлись ли
фолды на короткой базе. Если да — записать как подтверждённое свойство ASTR; если нет — как
свойство полной истории.

- [ ] **Step 5: Записать результат в `_comment` сетки**

Дописать после `РЕЗУЛЬТАТ ПРОГОНА 2026-08-30:` числа Steps 2–4 и вывод: идёт ли гейт в точку и на
какой базе.

- [ ] **Step 6: Коммит**

```bash
git add data/params/rsi_pullback/astr/cal_volume.json
git commit -m "feat(rsi_pullback): ASTR, тема volume прогнана"
```

---

### Task 8: Тема `vol_window` — окно объёмного гейта, седьмой замер оси в каталоге

**Files:**
- Modify: `data/params/rsi_pullback/astr/cal_vol_window.json` (только `_comment`)

**Interfaces:**
- Consumes: каталог из Task 1, реестр из Task 2.
- Produces: победителя `VolLookbackBars` — его читает Task 11.

- [ ] **Step 1: Прогнать тему**

```bash
go run ./cmd/backtest -ticker ASTR -strategy rsi_pullback -interval Minutes30 \
  -calibrate data/params/rsi_pullback/astr/cal_vol_window.json -out ./reports/ASTR_vol_window \
  -months 34 -train-months 10 -test-months 6 -min-trades 20 -metric profit_factor
```

- [ ] **Step 2: Выписать числа**

pooled OOS PF, сделки пула, разбивка по фолдам, выбор `VolLookbackBars` и `VolMult` в каждом фолде.

- [ ] **Step 3: Поместить результат в ряд каталога**

Это седьмой замер оси. Ряд: ELFV (18.9% дней короче 20 баров) — максимум на дефолте 3, BANEP
(18.4%) — на 16, DIAS (11.9%) — на 12, BSPB (7.7%) — на 5, YDEX (0.2%) — на 1, ASTR (1.5%) — на 2
по точечному замеру. Записать, подтвердил ли walk-forward узкий край, и какая редакция правила
BSPB о связи ликвидности с оптимальным окном выживает после седьмого замера.

- [ ] **Step 4: Проверить, не эквивалентен ли широкий гейт выключенному**

Если победило широкое окно (≥ 16), проверить точечным прогоном, режет ли гейт сделки вообще:
сравнить число сделок с baseline 158. Широкое окно ослабляет гейт, и при неразличимых числах
предпочитается выключенный гейт как более простая конфигурация (правило
`docs/rsi_pullback/strategy.md`).

- [ ] **Step 5: Записать результат в `_comment` сетки**

Дописать после `РЕЗУЛЬТАТ ПРОГОНА 2026-08-30:` числа Steps 2–4.

- [ ] **Step 6: Коммит**

```bash
git add data/params/rsi_pullback/astr/cal_vol_window.json
git commit -m "feat(rsi_pullback): ASTR, тема vol_window прогнана"
```

---

### Task 9: Тема `risk` — стоп, цель и проверка капкана

**Files:**
- Modify: `data/params/rsi_pullback/astr/cal_risk.json` (только `_comment`)

**Interfaces:**
- Consumes: каталог из Task 1, реестр из Task 2.
- Produces: победителей `StopDailyATR` и `TPDailyATR` — их читает Task 11.

- [ ] **Step 1: Прогнать тему**

```bash
go run ./cmd/backtest -ticker ASTR -strategy rsi_pullback -interval Minutes30 \
  -calibrate data/params/rsi_pullback/astr/cal_risk.json -out ./reports/ASTR_risk \
  -months 34 -train-months 10 -test-months 6 -min-trades 20 -metric profit_factor
```

- [ ] **Step 2: Выписать числа**

pooled OOS PF, сделки пула, разбивка по фолдам, выбор `StopDailyATR` и `TPDailyATR` в каждом фолде.

- [ ] **Step 3: Проверить победителя на капкан широкого стопа — ОБЯЗАТЕЛЬНО**

Это требование спеки (риск 3) и `_comment` сетки. На полной истории PF растёт вдвое (0.838 →
2.150) при неизменных 146–148 сделках — убыток вытесняется из стопа в RSI-выход.

Прогнать победивший стоп точечно и посчитать по журналу сделок долю выходов по SL:

```bash
cat > /tmp/astr_risk_check.json <<'EOF'
{"RSIPeriod":4,"RSILower":30,"RSIUpper":70,"EMAFast":10,"EMASlow":100,"DailyATRPeriod":14,"UseDayATRGate":1,"FreshDayATR":0,"SpentDayATR":0.8,"StopDailyATR":1.5,"TPDailyATR":0.6,"UseVolume":0,"VolBaseDays":14,"VolLookbackBars":3,"VolMult":1.2,"UseRSIExit":1,"UseTrail":0,"TrailDailyATR":0}
EOF
go run ./cmd/backtest -ticker ASTR -strategy rsi_pullback -interval Minutes30 \
  -params /tmp/astr_risk_check.json -out ./reports/ASTR_risk_check -months 34
```

Заменить `"StopDailyATR":1.5` на победивший стоп. Затем то же с `"StopDailyATR":0.5` (дефолт) в
`/tmp/astr_risk_base.json` и `-out ./reports/ASTR_risk_base`. Сравнить долю выходов по SL в журналах
сделок обоих отчётов.

Вывод записывается прямым текстом: если при победившем стопе доля выходов по SL резко ниже, а
число сделок то же — рост PF есть след капкана, и это пишется в `_comment`, в доку пакета и в
разбор Task 15. Скрывать этот вывод нельзя, даже если стоп всё равно идёт в точку.

- [ ] **Step 4: Проверить край цели**

Если победила цель ≥ 1.5, отметить, что колонки 1.5, 2.0 и 2.5 на полной истории совпадают
побайтово: цель шире ~1.5 дневного ATR на ASTR не достигается, и «победа» широкой цели означает
лишь, что цель не срабатывает вовсе, а выход делают RSI или стоп.

- [ ] **Step 5: Записать результат в `_comment` сетки**

Дописать после `РЕЗУЛЬТАТ ПРОГОНА 2026-08-30:` числа Steps 2–4, отдельной фразой — вывод по
капкану.

- [ ] **Step 6: Коммит**

```bash
git add data/params/rsi_pullback/astr/cal_risk.json
git commit -m "feat(rsi_pullback): ASTR, тема risk прогнана и проверена на капкан широкого стопа"
```

---

### Task 10: Темы `exit` и `trail` — выходы

**Files:**
- Modify: `data/params/rsi_pullback/astr/cal_exit.json` (только `_comment`)
- Modify: `data/params/rsi_pullback/astr/cal_trail.json` (только `_comment`)

**Interfaces:**
- Consumes: каталог из Task 1, реестр из Task 2.
- Produces: победителей `RSIUpper`, `UseRSIExit`, `UseTrail`, `TrailDailyATR` — их читает Task 11.

- [ ] **Step 1: Прогнать обе темы**

```bash
go run ./cmd/backtest -ticker ASTR -strategy rsi_pullback -interval Minutes30 \
  -calibrate data/params/rsi_pullback/astr/cal_exit.json -out ./reports/ASTR_exit \
  -months 34 -train-months 10 -test-months 6 -min-trades 20 -metric profit_factor

go run ./cmd/backtest -ticker ASTR -strategy rsi_pullback -interval Minutes30 \
  -calibrate data/params/rsi_pullback/astr/cal_trail.json -out ./reports/ASTR_trail \
  -months 34 -train-months 10 -test-months 6 -min-trades 20 -metric profit_factor
```

- [ ] **Step 2: Выписать числа обеих тем**

Для `exit`: pooled OOS PF, сделки пула, разбивка по фолдам, выбор `RSIUpper` в каждом фолде.
Для `trail`: то же плюс выбор `UseRSIExit` и `TrailDailyATR` в каждом фолде.

- [ ] **Step 3: Проверить, подтвердилось ли отрицательное ожидание по трейлу**

Точечные замеры: трейл 0.3 → 178 сделок/0.919, 0.5 → 161/0.958, 0.7 → 159/1.053, 1.0 и 1.3 →
158/1.093 (совпадает с baseline побайтово — не срабатывает ни разу). Записать, что выбрали фолды.
Если победил `TrailDailyATR ≥ 1.0` — это выбор «трейла нет», и в точку идёт `UseTrail = 0`, потому
что конфигурация без трейла проще и эквивалентна по результату.

- [ ] **Step 4: Проверить, не заменяет ли трейл RSI-выход**

Тема `trail` свипует `UseRSIExit` {0,1} именно ради этого вопроса. Выписать лучший результат при
`UseRSIExit = 0` и сравнить с лучшим при `UseRSIExit = 1`. Точечный замер: без RSI-выхода PF падает
до 1.003 против 1.093 — выход несёт работу, трейл его не заменяет.

- [ ] **Step 5: Записать результаты в `_comment` обеих сеток**

В каждый файл — после `РЕЗУЛЬТАТ ПРОГОНА 2026-08-30:` свои числа и вывод.

- [ ] **Step 6: Коммит**

```bash
git add data/params/rsi_pullback/astr/cal_exit.json data/params/rsi_pullback/astr/cal_trail.json
git commit -m "feat(rsi_pullback): ASTR, темы exit и trail прогнаны"
```

---

### Task 11: Сборка точки, её walk-forward и проверка стоп-условия

**Files:**
- Create: `data/params/rsi_pullback/astr/plateau_point.json`

**Interfaces:**
- Consumes: лидерборды всех десяти тем (Tasks 3–10).
- Produces: принятую точку — 18 полей `core.Params`, которые Task 12 переносит в литерал.

- [ ] **Step 1: Собрать точку из лидербордов тем**

Правило сборки: каждое поле берётся из темы, которая его меряет; **значение принимается, только
если за него высказались не менее трёх фолдов из четырёх**, иначе поле остаётся на дефолте ядра
(это цена широких сеток, названная в Global Constraints). Там, где выбор темы под остальными осями
точки не выживает, поле проверяется точечным прогоном и решение записывается в `_comment`.

Источники: `RSIPeriod`, `RSILower` — `cal_entry`; `RSIUpper` — `cal_exit` (тема `entry` свипует его
тоже, но `exit` меряет его отдельной осью); `EMAFast`, `EMASlow` — `cal_trend`; `FreshDayATR`,
`SpentDayATR` — `cal_day`/`cal_day_spent`; `UseVolume`, `VolMult`, `VolBaseDays`,
`VolLookbackBars` — `cal_volume`/`cal_vol_window`/`cal_screen`; `StopDailyATR`, `TPDailyATR` —
`cal_risk`; `UseRSIExit`, `UseTrail`, `TrailDailyATR` — `cal_trail`; `DailyATRPeriod` — 14, ось не
свипуется нигде.

- [ ] **Step 2: Создать файл точки**

`data/params/rsi_pullback/astr/plateau_point.json` — все 18 полей списками из ОДНОГО значения
(этого требует `TestRSIPullbackPlateauFilesArePoints`). Форма и обязательные разделы `_comment` — по
образцу `data/params/rsi_pullback/banep/plateau_point.json`: собственный путь файла; откуда взято
каждое поле и сколько фолдов за него высказались; оговорка, что точка собрана человеком, видевшим
всю историю, и потому её числа нельзя сравнивать с pooled OOS тем; вердикт по планке пункт за
пунктом; замеры Steps 3–7; полная команда запуска со схемой 34/10/6.

- [ ] **Step 3: Прогнать точку тем же walk-forward**

```bash
go run ./cmd/backtest -ticker ASTR -strategy rsi_pullback -interval Minutes30 \
  -calibrate data/params/rsi_pullback/astr/plateau_point.json -out ./reports/ASTR_point_oos \
  -months 34 -train-months 10 -test-months 6 -min-trades 20 -metric profit_factor
```

Выписать pooled OOS PF, сделки пула, PF и сделки каждого из четырёх фолдов.

- [ ] **Step 4: Проверить стоп-условие плана — пункты 1 и 2**

- pooled OOS PF < 1.0 → **СТОП**;
- меньше 20 сделок за расчётное окно → **СТОП**.

При срабатывании: числа приносятся владельцу, Tasks 12–15 НЕ выполняются.

- [ ] **Step 5: Проверить стоп-условие плана — пункт 3 (удвоенные издержки)**

```bash
go run ./cmd/backtest -ticker ASTR -strategy rsi_pullback -interval Minutes30 \
  -calibrate data/params/rsi_pullback/astr/plateau_point.json -out ./reports/ASTR_point_comm \
  -months 34 -train-months 10 -test-months 6 -min-trades 20 -metric profit_factor -commission 0.001
```

PF < 1.0 → **СТОП**. Записать процент потери PF. Для ASTR это рабочий фильтр, а не формальность:
baseline теряет 18.4% и уходит под единицу (1.093 → 0.892). Реальный круг издержек здесь 0.124%,
то есть удвоение примерно вшестеро строже реальности.

- [ ] **Step 6: Замерить плато соседями**

Прогнать точечно 4–6 конфигураций-соседей точки (по одному шагу сетки в каждую сторону вдоль
ведущих осей: `RSILower` ±1 шаг, `SpentDayATR` ±1 шаг, `StopDailyATR` ±1 шаг). Записать в
`_comment`: стоит ли точка на плато или на пике. Точка на пике — риск, и он записывается, а не
скрывается. На ASTR это особенно важно из-за монотонного рельефа входа: сосед со стороны более
глубокого уровня будет лучше по PF и хуже по выборке, и это надо показать числами.

- [ ] **Step 7: Проверить, что результат не держится одним полугодием**

Разбить сделки точки по шести полугодиям расчётного окна (2023-10-30 … 2024-04-18,
2024-04-18 … 2024-10-07, 2024-10-08 … 2025-03-28, 2025-03-31 … 2025-09-17,
2025-09-18 … 2026-03-09, 2026-03-09 … 2026-08-28) и записать вклад каждого. Обязательно из-за
риска 1 спеки: растёт **одно** полугодие из шести, и оно первое. Если больше половины
net-результата даёт одно полугодие — записать это прямо (прецедент IVAT, где 85% результата делала
одна неделя июля). Точка, чей net держится одним полугодием, вердикта не получает — это записано в
спеке до прогонов.

- [ ] **Step 8: Коммит**

```bash
git add data/params/rsi_pullback/astr/plateau_point.json
git commit -m "feat(rsi_pullback): ASTR, принятая точка и её замеры"
```

---

### Task 12: Литерал в пакете и снимок

**Files:**
- Modify: `internal/service/trading_strategy/rsi_pullback/strategy/astr/astr.go`
- Modify: `internal/service/trading_strategy/rsi_pullback/strategy/astr/astr_test.go`
- Modify: `internal/service/backtest/rsi_pullback_registry_test.go`

**Interfaces:**
- Consumes: точку из Task 11.
- Produces: `astr.DefaultParams()`, возвращающий литерал, — его читают Tasks 13 и 14.

- [ ] **Step 1: Заменить тест baseline снимком литерала**

В `astr_test.go` удалить `TestParamsTrackTheBaselineUntilCalibrated` и написать вместо него набор
тестов, где каждое утверждение несёт число из прогонов:

```go
func TestCalibratedLiteralIsPinned(t *testing.T) {
	want := core.Params{ /* 18 полей принятой точки из Task 11 */ }
	if got := DefaultParams(); got != want {
		t.Fatalf("откалиброванный литерал ASTR изменился:\n got: %+v\nwant: %+v", got, want)
	}
}

func TestParamsDoNotTrackTheBaseline(t *testing.T) {
	if DefaultParams() == core.DefaultParams() {
		t.Fatal("ASTR вернул core.DefaultParams(): откалиброванный тикер не должен отслеживать baseline")
	}
}

func TestStopIsArmed(t *testing.T) {
	if p := DefaultParams(); p.StopDailyATR <= 0 {
		t.Fatalf("StopDailyATR = %v, want > 0", p.StopDailyATR)
	}
}

func TestStopStaysAboveTheCostFloor(t *testing.T) {
	if p := DefaultParams(); p.StopDailyATR < 0.3 {
		t.Fatalf("StopDailyATR = %v: на стопе 0.3 ATR реальный круг издержек 0.124%% съедает 13.1%% риска, уже — ещё больше", p.StopDailyATR)
	}
}

func TestTargetIsArmed(t *testing.T) {
	if p := DefaultParams(); p.TPDailyATR <= 0 {
		t.Fatalf("TPDailyATR = %v, want > 0", p.TPDailyATR)
	}
}

func TestTargetStaysReachable(t *testing.T) {
	if p := DefaultParams(); p.TPDailyATR > 1.5 {
		t.Fatalf("TPDailyATR = %v: на ASTR цель шире 1.5 дневного ATR недостижима — колонки 2.0 и 2.5 замера совпадают побайтово", p.TPDailyATR)
	}
}

func TestTrendPairIsValid(t *testing.T) {
	if p := DefaultParams(); p.EMAFast >= p.EMASlow {
		t.Fatalf("EMAFast = %d >= EMASlow = %d: трендовый фильтр вырожден или инвертирован", p.EMAFast, p.EMASlow)
	}
}

func TestEntryBandIsBelowTheExitBand(t *testing.T) {
	if p := DefaultParams(); p.RSILower >= p.RSIUpper {
		t.Fatalf("RSILower = %v >= RSIUpper = %v: вход и выход перепутаны", p.RSILower, p.RSIUpper)
	}
}
```

Тест `TestTickerIsASTR` из Task 2 остаётся без изменений.

- [ ] **Step 2: Запустить тесты и убедиться, что они падают**

Run: `go test ./internal/service/trading_strategy/rsi_pullback/strategy/astr/ -v`
Expected: FAIL — пакет всё ещё возвращает baseline.

- [ ] **Step 3: Поставить литерал в пакет**

В `astr.go` заменить тело `DefaultParams()` литералом принятой точки и переписать доку пакета:
убрать блок «СОСТОЯНИЕ: КАЛИБРОВКА НЕ ПРОВОДИЛАСЬ», вписать вместо него результат — вердикт по
планке пункт за пунктом, pooled OOS PF точки и число сделок, пофолдовую разбивку, замер под
удвоенными издержками, разбивку по полугодиям, вывод по капкану широкого стопа из Task 9 Step 3,
оговорку о подглядывании. Если планка не взята — написать это прямым текстом вместе с принятым
риском (правило прода из Global Constraints). Остальные блоки доки (окно, схема, кэш, априор,
свойства инструмента) сохранить.

```go
// DefaultParams returns the calibrated literal accepted on 2026-08-30.
func DefaultParams() core.Params {
	return core.Params{ /* 18 полей принятой точки */ }
}
```

- [ ] **Step 4: Заменить сторожевой тест реестра снимком**

В `internal/service/backtest/rsi_pullback_registry_test.go` удалить
`TestRSIPullbackASTRTracksBaseline` и написать вместо него
`TestRSIPullbackASTRIsRegisteredAndCalibrated` по образцу
`TestRSIPullbackBANEPIsRegisteredAndCalibrated`: тикер обязан быть в `rsiPullbackRegistry`, его
параметры обязаны совпадать с `rsipullbackastr.DefaultParams()` и не совпадать с
`core.DefaultParams()`.

- [ ] **Step 5: Запустить тесты**

Run: `go test ./internal/service/trading_strategy/rsi_pullback/... ./internal/service/backtest/ -v`
Expected: PASS.

- [ ] **Step 6: Коммит**

```bash
git add internal/service/trading_strategy/rsi_pullback/strategy/astr internal/service/backtest
git commit -m "feat(rsi_pullback): ASTR откалиброван — литерал вместо отслеживания baseline"
```

---

### Task 13: Реестр живого раннера

**Files:**
- Modify: `internal/service/trading_strategy/rsi_pullback/live/registry.go`
- Modify: `internal/service/trading_strategy/rsi_pullback/live/registry_test.go`

**Interfaces:**
- Consumes: `astr.Ticker`, `astr.DefaultParams()` из Task 12.
- Produces: запись в `paramsByTicker` — её читает `ParamsFor` живого раннера и `cmd/pullparity`.

- [ ] **Step 1: Добавить импорт и запись в карту**

Импорт `"tinvest/internal/service/trading_strategy/rsi_pullback/strategy/astr"` и строка в
`paramsByTicker` рядом с `banep.Ticker`:

```go
	astr.Ticker: astr.DefaultParams(),
```

- [ ] **Step 2: Дописать комментарий карты**

Перед картой в `registry.go` стоят абзацы про каждый заведённый тикер. Дописать абзац про ASTR на
английском, в стиле соседних: адаптированная схема 34/10/6 и почему (IPO 2023-10-13, истории 34.5
месяца), решение о максимально широких сетках, вердикт по планке, предпоследний baseline каталога
(1.093), каноническая процедура тем, ликвидность и отсутствие дивидендного риска как снятые риски,
самый тяжёлый режим каталога и капкан широкого стопа как остаточные риски.

- [ ] **Step 3: Запустить тесты пакета**

Run: `go test ./internal/service/trading_strategy/rsi_pullback/live/... -v`
Expected: PASS. В пакете есть два теста, которые прямо сторожат эту пару задач:
`TestEveryDefaultTickerIsRegistered` (каждый тикер боевой вселенной обязан быть в карте) и
`TestBaselineTrackingTickersStayOutOfTheDefaultUniverse` (тикер, всё ещё отслеживающий baseline, в
боевую вселенную попасть не может). Второй — причина, по которой Task 12 идёт раньше Task 14.

- [ ] **Step 4: Коммит**

```bash
git add internal/service/trading_strategy/rsi_pullback/live
git commit -m "feat(rsi_pullback): ASTR в реестре живого раннера"
```

---

### Task 14: Боевая вселенная

**Files:**
- Modify: `internal/config/rsi_pullback.go`
- Modify: `internal/config/rsi_pullback_test.go` (список `want` в тесте дефолта вселенной)
- Modify: `env/prod.env`, `env/prod.env.example`, `env/local.env.example`
- Modify: `docs/rsi_pullback/live.md` (§8, таблица боевой вселенной)

**Interfaces:**
- Consumes: литерал из Task 12, реестр из Task 13.
- Produces: `RSI_PULLBACK_TICKERS` из девятнадцати тикеров.

- [ ] **Step 1: Проверить стоп-условие ещё раз**

Прежде чем заводить тикер в прод, перечитать числа Task 11 Steps 4–5. Все три пункта стоп-условия
не сработали — иначе эта задача не выполняется вовсе.

- [ ] **Step 2: Обновить тест дефолта**

В `internal/config/rsi_pullback_test.go:54` в список `want` добавить `"ASTR"` девятнадцатым.

- [ ] **Step 3: Запустить тест и убедиться, что падает**

Run: `go test ./internal/config/ -v`
Expected: FAIL — дефолт ещё из восемнадцати тикеров.

- [ ] **Step 4: Обновить дефолт и env-файлы**

В `internal/config/rsi_pullback.go` дописать `"ASTR"` в конец списка `Tickers` (строка 233) и
добавить перед ним комментарий-абзац в стиле соседних (BANEP, YDEX, BSPB): когда заведён, вердикт
по планке, числа точки, **тип риска** (у ASTR он не исполнительный, как у ELFV, и не дивидендный,
как у BSPB и BANEP, а риск режима — самый тяжёлый в каталоге — и риск монотонного рельефа входа),
каноническая процедура тем, адаптированная схема окна и решение о широких сетках.

Во всех трёх env-файлах дописать `,ASTR` в конец `RSI_PULLBACK_TICKERS`. Текущее значение:
`UGLD,T,GAZP,DOMRF,FESH,WUSH,LENT,RENI,NVTK,LSNGP,IVAT,SVAV,SIBN,ELFV,DIAS,BSPB,YDEX,BANEP`.

- [ ] **Step 5: Обновить `live.md` §8**

Дописать ASTR в таблицу боевой вселенной: тикер, дата заведения, взята ли планка, pooled OOS PF
точки и число сделок, ключевой риск, схема прогонов (34/10/6 — отметить отдельно, потому что она
отличается от штатной).

- [ ] **Step 6: Запустить тесты**

Run: `go test ./internal/config/ ./internal/service/trading_strategy/rsi_pullback/... -v`
Expected: PASS.

- [ ] **Step 7: Коммит**

```bash
git add internal/config env docs/rsi_pullback/live.md
git commit -m "feat(rsi_pullback): завести ASTR в боевую вселенную"
```

---

### Task 15: Документация — разбор калибровки и принятый риск

**Files:**
- Modify: `docs/rsi_pullback/strategy.md`
- Modify: `docs/rsi_pullback/live.md`

**Interfaces:**
- Consumes: все числа Tasks 3–11.
- Produces: строку ASTR в справочнике стратегии и риск в `live.md` §10.

Правило CLAUDE.md: `docs/rsi_pullback/*` описывает **механику**, а не пер-тикерные разборы.
Поэтому здесь пишутся только строка в таблицу откалиброванных тикеров, механические выводы (то, что
меняет понимание процедуры для следующего тикера) и риск живого раннера. Развёрнутый разбор ASTR
живёт в доке пакета `strategy/astr` и в `_comment` сеток.

- [ ] **Step 1: Дописать строку в таблицу §8.0.1 `strategy.md`**

Строка ASTR: схема прогонов (34/10/6 — адаптированная), вердикт по планке, pooled OOS PF точки и
сделки, дата 2026-08-30.

- [ ] **Step 2: Дописать механические выводы**

Одним коротким абзацем — только то, что меняет процедуру для следующего тикера, максимум три
пункта из перечисленных ниже (те, что реально подтвердились):

1. **Седьмой замер оси `vol_window`** — какая редакция правила BSPB о связи ликвидности и
   оптимального окна выжила после ASTR (1.5% коротких дней, точечный максимум на 2).
2. **Второй тикер с широкими сетками** — что расширение осей изменило по сравнению с тем, что
   изменило бы на узких (у BANEP оно изменило ровно одну ось — риск). Если на ASTR картина другая,
   это уже правило, а не единичный случай.
3. **Ось `VolBaseDays` как значимая** — второй случай в каталоге после отрицательного результата
   BANEP; если walk-forward подтвердил, ось перестаёт считаться шумовой по умолчанию.

Одноразовые числа ASTR (PF конкретных конфигураций, полугодия, режим) сюда **не** переносятся —
они в доке пакета.

- [ ] **Step 3: Дописать риск в `live.md` §10**

Новый риск номером, следующим за последним занятым — **режим и рельеф входа ASTR**:

- самый тяжёлый режим боевой вселенной: итог расчётного окна −57.7%, максимальная просадка −82.9%,
  растёт одно полугодие из шести. Лонговый результат получен против режима, и живая просадка может
  оказаться глубже бэктестовой;
- рельеф входа монотонный (чем глубже `RSILower`, тем выше PF на полной истории) — риск
  переподгонки выше обычного; защита — правило «три фолда из четырёх», применённое при сборке
  точки;
- условие пересмотра: первая живая просадка глубже той, что дала точка на бэктесте, либо два
  подряд убыточных квартала живой торговли.

Отдельным пунктом дописать, что **дивидендный риск и исполнительный риск у ASTR сняты**: гэпов
хуже −3% за 34 месяца ровно один и он рыночный, оборот p10 85 млн ₽ при гейте вселенной 50 млн,
дней короче 20 баров 1.5%. Календарь отсечек, обязательный для BANEP, этому тикеру не нужен.

- [ ] **Step 4: Проверить, что доки не противоречат числам**

Перечитать написанное рядом с `_comment` сеток и докой пакета: одни и те же числа обязаны совпадать
во всех трёх местах. Расхождение — ошибка, а не стилистика (на SVAV, DIAS и BSPB такие расхождения
находились и исправлялись отдельными коммитами).

- [ ] **Step 5: Коммит**

```bash
git add docs/rsi_pullback
git commit -m "docs(rsi_pullback): ASTR в справочнике стратегии и принятый риск"
```

---

### Task 16: Финальная проверка

**Files:** нет изменений, только проверки (кроме Step 3).

- [ ] **Step 1: Полный гейт качества**

Run: `./bin/mage ci`
Expected: lint + `go test -race ./...` + проверка дрейфа моков — зелёные.

- [ ] **Step 2: Сверка живой сборки с бэктестом**

```bash
go run ./cmd/pullparity -tickers ASTR -months 24
```

Expected: ноль расхождений. **24 месяца, а не 34:** живой сборщик тянет дневные свечи окном
`dailyFetchDays = 730` (`live/marketdata/marketdata.go:47`), и на большем горизонте появляются
ожидаемые расхождения длины `Daily*` рядов (`maxDailyHorizonMonths`, выяснено на IVAT). Расхождение
на 24 месяцах означает, что живой раннер и бэктест считают сигнал по-разному, и заведение в прод
откатывается до выяснения.

- [ ] **Step 3: Записать результат сверки в `live.md` §9**

Строка вида «ASTR заведён 2026-08-30 и сверяется за 24 месяца (`go run ./cmd/pullparity -tickers
ASTR -months 24` — <N> баров, **ноль расхождений**)» рядом с такими же строками про IVAT, SVAV,
SIBN, ELFV, DIAS, BSPB, YDEX и BANEP. Коммит: `docs(rsi_pullback): сверка ASTR — 24 месяца, ноль
расхождений`.

- [ ] **Step 4: Сообщить владельцу результат**

Короткий отчёт: вердикт по планке пункт за пунктом; замеры принятой точки (pooled OOS, пофолдовая
разбивка, удвоенные издержки, плато, разбивка по полугодиям); что заведено в прод; какие риски
записаны; что осталось (первые живые сделки, условия пересмотра). Тремя отдельными строками:

1. **устоял ли монотонный рельеф входа** — выбрали ли фолды глубокие уровни `RSILower`, и оказался
   ли самый чистый сигнал каталога свойством бумаги или свойством полной истории;
2. **что дал капкан широкого стопа на дешёвой бумаге** — какова доля выходов по SL при победившем
   стопе против дефолтного, и сколько роста PF настоящее;
3. **что изменили широкие сетки на втором тикере подряд** — совпала ли картина с BANEP (изменилась
   ровно одна ось) или расширение сработало иначе.

---

## Self-review

**Покрытие спеки.** Окно 34/10/6 и его обоснование → Global Constraints, дока пакета (Task 2
Step 3), сторожевой тест (Task 1 Step 1), каждая команда прогона, Task 14 Step 5; кэш и запас 16
дней → Global Constraints, дока пакета, Task 2 Step 7; априор скринера и baseline → дока пакета,
Task 2 Step 7; каноническая процедура тем → Architecture, Task 1 (все десять файлов сразу), дока
пакета; решение о максимально широких сетках → Global Constraints, сторож Task 1, `_comment` каждой
сетки, дока пакета, правило сборки Task 11 Step 1; Свойство 1 (режим) → дока пакета, Task 11
Step 7, Task 15 Step 3; Свойство 2 (издержки) → `_comment` `cal_risk`, Task 11 Step 5, Task 12
Step 1 (`TestStopStaysAboveTheCostFloor`); Свойство 3 (ликвидность) → дока пакета, `_comment`
`cal_vol_window`, Task 15 Step 3; Свойство 4 (нет дивидендного риска) → дока пакета, Task 13
Step 2, Task 15 Step 3; Свойство 5 (трендовый допуск, вырожденные пары) → сторож Task 1, `_comment`
`cal_trend`, Task 5 Step 3, Task 12 Step 1 (`TestTrendPairIsValid`); Свойство 6 (ATR, выживаемость,
капкан) → `_comment` `cal_risk`, Task 9 Step 3; Свойство 7 (цель) → `_comment` `cal_risk`, Task 9
Step 4, Task 12 Step 1 (`TestTargetStaysReachable`); Свойство 8 (дневной гейт) → `cal_day`,
`cal_day_spent`, Task 6; Свойство 9 (объёмный гейт, значимая база) → `cal_volume`, Task 7 Step 4,
сторож Task 1; Свойство 10 (окно гейта) → `cal_vol_window`, Task 8 Step 3; Свойство 11 (монотонный
вход) → `cal_entry`, Task 4 Step 3, Task 11 Step 6, Task 16 Step 4 пункт 1; Свойство 12 (выход и
трейл) → `cal_exit`, `cal_trail`, Task 10 Steps 3–4; планка → Global Constraints, вердикт выносится
в Tasks 4, 5, 11, 12, 15; стоп-условие → Global Constraints, Task 11 Steps 4–5, Task 14 Step 1;
правило прода → Global Constraints, Task 12 Step 3, Task 14; артефакты спеки → задачи 1, 2, 11, 12,
13, 14, 15; порядок работы спеки → порядок задач; риск 1 (режим) → Task 11 Step 7; риск 2 (тонкое
обучающее окно) → Global Constraints, `_comment` `cal_entry` и `cal_day`, Task 6 Step 4; риск 3
(капкан) → Task 9 Step 3; риск 4 (переподгонка входа) → Task 4 Step 3, Task 11 Steps 1 и 6; риск 5
(издержки) → Task 11 Step 5; риск 6 (несравнимость с каталогом) → дока пакета, Task 14 Step 5,
Task 15 Step 1; риск 7 (малозначимая `vol_window`) → `_comment` `cal_vol_window`, Task 8.

**Плейсхолдеры.** В задачах 3–11 значения результатов прогонов не выдуманы: там, где число станет
известно только после запуска, стоит явное указание выписать его из отчёта — это данные, которых до
прогона не существует, а не плейсхолдер плана. Комментарии `/* 18 полей принятой точки */` в
Task 12 — единственная подстановка, и её содержимое определяется правилом сборки Task 11 Step 1;
раньше Task 11 этих значений физически не существует. Код сторожевого теста, теста baseline, доки
пакета и всех десяти сеток дан целиком.

**Согласованность имён.** `astrGrid` определён и используется только в
`rsi_pullback_astr_grid_test.go` (Task 1); `TestASTRGridsStayWide` — единственный тест этого файла.
`TestRSIPullbackASTRTracksBaseline` (Task 2 Step 5) заменяется на
`TestRSIPullbackASTRIsRegisteredAndCalibrated` (Task 12 Step 4);
`TestParamsTrackTheBaselineUntilCalibrated` (Task 2 Step 1) заменяется на
`TestCalibratedLiteralIsPinned` (Task 12 Step 1); `TestTickerIsASTR` (Task 2 Step 1) переживает
Task 12 без изменений. `astr.Ticker` и `astr.DefaultParams()` объявлены в Task 2 и потребляются в
Tasks 12–14. Импорт в реестре бэктеста назван `rsipullbackastr` — как соседние `rsipullbackbanep` и
`rsipullbackbspb`; в живом реестре — `astr`, как соседний `banep`.

**Арифметика сеток.** 4 + 378 + 27 + 63 + 10 + 35 + 40 + 48 + 7 + 14 = **626 прогонов** — совпадает
с числом в спеке.
