# Scalping RSI+MACD (5m/15m/30m, лонг)

Дизайн: `docs/superpowers/specs/2026-07-22-scalping-rsimacd-design.md`
Код: `internal/service/trading_strategy/scalping_rsimacd/strategy/core`

## Правила

**Вход** (только лонг, только внутри сессии Пн–Чт 08:00–17:00 MSK, Пт 08:00–14:00):

1. На текущем баре MACD(3,6,9) пересекает сигнальную линию снизу вверх, **обе линии ниже нуля**.
2. На баре кросса RSI(`RSIPeriod`) выше порога `RSIEntryMin` (0 — фильтр выключен;
   по умолчанию 50).
3. Тренд старшего таймфрейма: последнее закрытие завершённой **часовой** свечи выше
   `EMA(H1, HTFTrendEMA)`. `HTFTrendEMA = 0` выключает фильтр; по умолчанию 50.
   Фильтр **fail-closed**: если включён, а часовой истории меньше `HTFTrendEMA` баров,
   вход запрещён.
4. В окне из `MACDConfirmBars` баров до текущего (включая сам бар кросса) RSI(`RSIPeriod`)
   пересёк уровень `RSIOversold` снизу вверх и закрылся выше него.
5. Структурный уровень (минимум свечи RSI-кросса минус `StopBufferATR×ATR`) держался
   с момента RSI-кросса, а закрытие текущего бара выше него. Этот гейт работает в обоих
   режимах стопа: сетап, чей лоу уже пробит, мёртв независимо от того, где стоит стоп.

**Стоп и тейк отмеряются в ATR от цены входа:**

- стоп = `вход − StopATR×ATR` (по умолчанию `StopATR = 1.0`);
- тейк = `вход + TPATR×ATR` (по умолчанию `TPATR = 2.0`).

ATR — Уайлдера, период `ATRPeriod` (14), считается по свечам **базового** таймфрейма
(`-interval`), на текущем баре.

Нулевое значение возвращает прежнюю геометрию: `StopATR = 0` → стоп по структурному
уровню, `TPATR = 0` → тейк `вход + RR × риск`. Границы `[MinRiskATR×ATR, MaxRiskATR×ATR]`
проверяются только в режиме структурного стопа — в ATR-режиме риск равен `StopATR×ATR`
по построению, и та же проверка выродилась бы в константу от параметров.

**Выходы** в порядке приоритета:

1. `SL` — стоп задет лоу бара.
2. `TP` — тейк задет хаем бара.
3. `STOCH` — %K классического стохастика (14,3,3) выходит вниз из зоны 80; закрытие по цене
   закрытия бара.
4. `ATR` — волатильность входит в критическую зону **сверху вниз**: отношение
   `ATR / SMA(ATR, ATRAvgPeriod)` на прошлом баре было ≥ `ATRExitRatio`, а на текущем стало
   меньше. Закрытие по цене закрытия бара. `ATRExitRatio = 0` выключает выход;
   по умолчанию 0.8 при `ATRAvgPeriod = 50`.
5. `EOD` — принудительное закрытие на последнем баре сессии; позиция не переносится через
   ночь и выходные.

ATR — величина в рублях, а не осциллятор в диапазоне 0–100, поэтому «критическая зона»
задана не абсолютным уровнем, а отношением текущего ATR к его собственной средней за
`ATRAvgPeriod` баров. Единица — «волатильность как обычно»; 0.8 — «волатильность упала до
80% от своей нормы». Такой порог не зависит ни от инструмента, ни от таймфрейма.

Сигналом служит именно **пересечение** уровня, а не сам факт низкой волатильности: позицию,
открытую в уже спокойном рынке, иначе закрывало бы на первом же баре. При нехватке истории
для базы SMA выход молчит — позицию по-прежнему защищают стоп, тейк и закрытие на конец дня.

Часовые свечи для фильтра тренда грузит `cmd/backtest` (интервал `Hour1`, годичный
lead-in для прогрева EMA); движок видит только полностью закрытые H1-бары
(`Config.HTFInterval = 1h`), заглядывания вперёд нет.

Стратегии всегда нужна H1-серия (даже при `HTFTrendEMA = 0` `cmd/backtest` её загружает).
На холодном кэше первый прогон должен идти с `-refresh`, иначе провайдер отдаст пустую
или короткую голову H1-серии — `cmd/backtest` в этом случае либо падает с ошибкой (пустая
серия), либо печатает предупреждение с фактическим разрывом (короткая голова).

## Запуск

Таймфрейм задаётся флагом `-interval`: поддерживаются `Minutes5` (референсный),
`Minutes15` и `Minutes30`. Правила от таймфрейма не зависят — длину бара стратегия
выводит из самих данных (медиана интервалов между барами), поэтому EOD-выход всегда
приходится на последний бар, целиком помещающийся в сессию. Фильтр тренда остаётся
часовым на любом из этих интервалов.

Калибровка (walk-forward, основной прогон):

```
go run ./cmd/backtest -ticker SBER -strategy scalping_rsimacd -interval Minutes5 \
  -calibrate data/params/scalping_rsimacd/grid.json -out ./reports/SBER \
  -months 24 -test-months 6 -min-trades 20 -metric profit_factor -refresh
```

`-refresh` обязателен на первом прогоне: часовой кэш в `data/candles` начинается
с 2024-07-17, а провайдер досыпает только хвост серии, но не голову. Без `-refresh`
H1-лид-ина не хватит на прогрев `EMA(100)`, и fail-closed гейт молча зарежет входы
в ранних фолдах. CLI печатает покрытие H1 (число баров, первый и последний бар) и
предупреждает, если голова всё равно короткая.

Разведочный прогон без калибровки (на дефолтах: `RSIEntryMin = 50`, `HTFTrendEMA = 50`,
`StopATR = 1.0`, `TPATR = 2.0`):

```
go run ./cmd/backtest -ticker SBER -strategy scalping_rsimacd -interval Minutes5 \
  -out ./reports/SBER -months 6 -refresh
```

Диагностика одного бара: добавить `-explain "2026-07-20 12:35"` (время MSK). `-explain`
учитывает `-params`: если указать `-params <файл>.json`, разбор гейтов на бар пойдёт по
этой (например, откалиброванной) конфигурации, а не по дефолтам — так можно
продиагностировать конкретный кандидат с включённым `HTFTrendEMA`.

Грид `data/params/scalping_rsimacd/grid.json` состоит из трёх фаз: `entry` (геометрия
входа), `context` (`RSIEntryMin` × `HTFTrendEMA` — оба гейта включаются и выключаются
значением 0), `risk` (`StopATR` × `TPATR` — геометрия сделки в ATR) и `exits`
(аблация стохастик-выхода × `ATRExitRatio`). Всего 27 + 6×12 + 6×9 + 6×6 = 189 комбинаций.

## Критерий приёмки

Решение принимается только по pooled OOS profit factor на walk-forward: PF ≥ 1.5 при
≥ 30 OOS-сделок — кандидат на live; 1.0–1.5 — edge не подтверждён; сходимость всех комбо
или < 10 сделок — сетап слишком редок, диагностировать через `-explain`.

## Разведочный прогон (single-run, defaults, 2026-07-22)

> Цифры ниже сняты ДО гейтов от 2026-07-23 (`RSIEntryMin`, `HTFTrendEMA`) — на тогдашних
> дефолтах `RSIEntryMin = 0` (фильтр выключен). Сейчас `RSIEntryMin` по умолчанию равен
> 50, что само по себе отсекает примерно треть исходной выборки, так что этот прогон
> больше не воспроизводим на текущих дефолтах и приведён только как исторический
> ориентир.

`go run ./cmd/backtest -ticker SBER -strategy scalping_rsimacd -interval Minutes5 -out ./reports/SBER -months 6`
(defaults из `core.DefaultParams()`, без калибровки) дал 533 сделки за 6 месяцев,
profit factor 0.158, win rate 16.5%, чистый PnL −45.21%. Это не повод менять пороги
(они зафиксированы спекой) — калибровочный грид (`data/params/scalping_rsimacd/grid.json`)
и walk-forward должны решить, есть ли здесь эдж на дефолтных геометриях вообще, а не этот
разведочный прогон.
